# Web — Memory

This file accumulates durable knowledge Web learns across runs.

Maintain these categories. Append entries only when you see the same
pattern twice or encounter something genuinely non-obvious — not every
run produces a memory.

- **App boot patterns** — how this repo's dev server is actually started (script name, port, env vars, common gotchas).
- **Repro shortcuts** — quick ways to land in a known state (test creds, seed routes, debug query params).
- **Patterns that worked** — diagnostic approaches that produced fast, correct answers.
- **Patterns that failed** — approaches to avoid, with reason.
- **Console-error signatures** — recurring red messages and what they actually indicate.

(Empty on first run.)
