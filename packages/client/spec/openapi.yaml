# The Ownware gateway wire contract — REST surface, v1.
#
# This file is the versioned PROMISE @ownware/client is built against.
# Scope is deliberate: only the versioned public operations below. The
# gateway exposes many more routes (profiles CRUD, connectors,
# schedules, …) — those are the management surface, INTERNAL and
# unstable until they appear here.
#
# Companion: asyncapi.yaml documents the SSE event vocabulary.

openapi: 3.1.0
info:
  title: Ownware Gateway — core wire contract
  version: 0.37.0
  description: >
    Drive a Ownware agent from any language: start a run, stream its
    events, answer permission prompts. Bearer auth applies to every
    route except /health whenever the gateway has auth enabled (always
    true on non-loopback binds; token at <dataDir>/gateway-token).

servers:
  - url: http://localhost:3011
    description: Default local gateway (ownware serve prints yours)

security:
  - bearer: []

paths:
  /api/v1/health:
    get:
      operationId: health
      security: []  # the ONE unauthenticated route
      summary: Liveness.
      responses:
        '200':
          description: Gateway is up.
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status: { type: string, const: ok }
                  version: { type: string }
                  uptime: { type: number }

  /api/v1/capabilities:
    get:
      operationId: capabilities
      summary: Discover the deliberately published Gateway contract.
      responses:
        '200':
          description: Public contract version and supported capabilities.
          content:
            application/json:
              schema:
                type: object
                required: [contract, capabilities, limits]
                properties:
                  contract:
                    type: object
                    required: [name, major, revision]
                    properties:
                      name: { type: string, const: ownware.gateway }
                      major: { type: integer, const: 1 }
                      revision: { type: string }
                  capabilities:
                    type: array
                    items:
                      type: object
                      required: [id, version]
                      properties:
                        id: { type: string }
                        version: { type: integer, minimum: 1 }
                  limits: { $ref: '#/components/schemas/PublicGatewayLimits' }
        '401': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/connections:
    get:
      operationId: listConnections
      summary: List the install owner's provider-neutral connection inventory.
      description: >
        Requires an authenticated install owner; delegated principals cannot
        enumerate connection state. Only the latest material state per logical
        capability is returned. Provider identity, vendor account handles,
        credential/session material, raw errors and explicitly revoked history
        are never projected. A connection is not permission: every use still
        requires a separate scoped grant and live evaluator decision.
      parameters:
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
        - in: query
          name: cursor
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Bounded content-free inventory with fixed recovery guidance.
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ConnectionList' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/task-catalog:
    get:
      operationId: listTaskCatalog
      summary: List installed immutable task packs and their scope decisions.
      description: >
        Install-owner-only. Task metadata describes outcomes while package
        digests establish byte identity only; they do not prove publisher
        identity, permission, or task completion. Without context parameters,
        effectiveVersion and tasks reflect global policy only.
      parameters:
        - in: query
          name: workspaceId
          schema: { type: string, minLength: 1, maxLength: 256 }
        - in: query
          name: agentId
          schema: { type: string, minLength: 1, maxLength: 256 }
      responses:
        '200':
          description: Task-oriented catalog and revisioned global/workspace/agent decisions.
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TaskPackCatalog' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/task-catalog/{taskPackId}/scope:
    put:
      operationId: setTaskPackScope
      summary: Compare-and-set one task-pack scope decision and selected version.
      description: >
        Install-owner-only. A deny selects no version. An allow must select one
        installed immutable version. The expected revision prevents lost updates.
      parameters:
        - in: path
          name: taskPackId
          required: true
          schema:
            type: string
            pattern: '^[a-z0-9](?:[a-z0-9._-]{0,126}[a-z0-9])?$'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SetTaskPackScopeInput' }
      responses:
        '200':
          description: The actual durable scope decision and its new or replayed revision.
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema:
                type: object
                required: [scope]
                properties:
                  scope: { $ref: '#/components/schemas/TaskPackScope' }
                additionalProperties: false
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/provider-hub:
    get:
      operationId: getProviderHubOverview
      summary: Read the central provider, route, model, price and connection counts.
      responses:
        '200':
          description: Secret-free active catalog generation and health.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProviderHubOverview' }
        '401': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/provider-hub/providers:
    get:
      operationId: listProviderHubProviders
      summary: List provider families and their runnable or catalog-only routes.
      responses:
        '200':
          description: Provider summaries from the active catalog generation.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProviderHubProviderPage' }
        '401': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/provider-hub/connections:
    get:
      operationId: listProviderHubConnections
      summary: List secret-free API-key, subscription, local and custom connections.
      responses:
        '200':
          description: Connections projected from credential metadata and runtime-owned state.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProviderHubConnectionPage' }
        '401': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/provider-hub/verifications:
    get:
      operationId: getProviderHubVerifications
      summary: Read independently versioned provider-route verification evidence.
      responses:
        '200':
          description: Secret-free evidence bundle, or null before a harness run.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProviderHubVerificationOverview' }
        '401': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/provider-hub/models:
    get:
      operationId: listProviderHubModels
      summary: Search and page the central model catalog without overstating verification.
      parameters:
        - { in: query, name: q, schema: { type: string, minLength: 1, maxLength: 500 } }
        - { in: query, name: providerFamilyId, schema: { type: string, minLength: 1, maxLength: 512 } }
        - { in: query, name: providerRouteId, schema: { type: string, minLength: 1, maxLength: 512 } }
        - { in: query, name: connectionId, schema: { type: string, minLength: 1, maxLength: 512 } }
        - { in: query, name: lifecycle, schema: { type: string, enum: [active, experimental, deprecated, hidden] } }
        - { in: query, name: scope, schema: { type: string, enum: [all, connectable, connected, verified, recommended] } }
        - { in: query, name: limit, schema: { type: integer, minimum: 1, maximum: 200, default: 50 } }
        - { in: query, name: cursor, schema: { type: string, minLength: 1, maxLength: 4096 } }
      responses:
        '200':
          description: One generation-bound model page with applicable price scopes.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProviderHubModelPage' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/provider-hub/health:
    get:
      operationId: getProviderHubHealth
      summary: Read active Provider Hub generation health.
      responses:
        '200':
          description: The same generation and health envelope as the overview.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProviderHubOverview' }
        '401': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/provider-hub/usage:
    get:
      operationId: listProviderHubUsage
      summary: List immutable provider-call facts with their latest cost observation.
      description: Latest is selected by adapter-assigned append sequence, never caller timestamp.
      parameters:
        - { in: query, name: from, schema: { type: string, format: date-time } }
        - { in: query, name: until, schema: { type: string, format: date-time } }
        - { in: query, name: profileId, schema: { type: string, minLength: 1, maxLength: 512 } }
        - { in: query, name: threadId, schema: { type: string, minLength: 1, maxLength: 512 } }
        - { in: query, name: classification, schema: { $ref: '#/components/schemas/ProviderHubCostClassification' } }
        - { in: query, name: limit, schema: { type: integer, minimum: 1, maximum: 1000, default: 100 } }
      responses:
        '200':
          description: Bounded provider usage page.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProviderHubUsagePage' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/provider-hub/usage/summary:
    get:
      operationId: summarizeProviderHubUsage
      summary: Summarize tokens and latest costs without blending classifications.
      parameters:
        - { in: query, name: from, schema: { type: string, format: date-time } }
        - { in: query, name: until, schema: { type: string, format: date-time } }
        - { in: query, name: profileId, schema: { type: string, minLength: 1, maxLength: 512 } }
        - { in: query, name: threadId, schema: { type: string, minLength: 1, maxLength: 512 } }
      responses:
        '200':
          description: Classification-separated usage summary.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProviderHubUsageSummary' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/provider-hub/usage/export:
    get:
      operationId: exportProviderHubUsageEvidence
      summary: Export every immutable fact, append-only observation and hashed price snapshot.
      responses:
        '200':
          description: Lossless secret-free provider usage evidence.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProviderHubUsageEvidenceExport' }
        '401': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/provider-hub/usage/{usageId}/cost-observations:
    post:
      operationId: reconcileProviderHubUsageCost
      summary: Append an operator reconciliation without replacing earlier evidence.
      parameters:
        - in: path
          name: usageId
          required: true
          schema: { type: string, minLength: 1, maxLength: 512 }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ProviderHubReconciledCostInput' }
      responses:
        '201':
          description: Usage entry projected with the appended reconciliation as latest.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProviderHubUsageEntry' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/provider-hub/catalog/refresh:
    post:
      operationId: refreshProviderHubCatalog
      summary: Refresh the remote catalog while retaining last-known-good state on failure.
      parameters:
        - in: query
          name: force
          schema: { type: string, enum: ['true', 'false'], default: 'false' }
      responses:
        '200':
          description: Fresh active generation.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProviderHubOverview' }
        '202':
          description: Refresh did not produce a fresh generation; last-known-good remains active.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProviderHubOverview' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/provider-hub/connections/openai-compatible:
    get:
      operationId: listOpenAICompatibleConnections
      summary: List persisted secret-free OpenAI-compatible endpoint configurations.
      responses:
        '200':
          description: Custom endpoint configurations; plaintext keys never appear.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OpenAICompatibleConnectionList' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
    post:
      operationId: saveOpenAICompatibleConnection
      summary: Create or update one runtime-registered OpenAI-compatible endpoint.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/OpenAICompatibleConnectionInput' }
      responses:
        '200':
          description: Updated secret-free configuration.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OpenAICompatibleConnectionConfig' }
        '201':
          description: Created secret-free configuration.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OpenAICompatibleConnectionConfig' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/provider-hub/connections/openai-compatible/{connectionId}:
    delete:
      operationId: deleteOpenAICompatibleConnection
      summary: Remove a compatible endpoint and its dedicated managed credential.
      parameters:
        - { in: path, name: connectionId, required: true, schema: { type: string, pattern: '^oai_[a-f0-9]{12}$' } }
      responses:
        '200':
          description: Removal confirmation.
          content:
            application/json:
              schema:
                type: object
                required: [removed]
                properties: { removed: { type: boolean, const: true } }
                additionalProperties: false
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/provider-hub/connections/openai-compatible/{connectionId}/discover:
    post:
      operationId: discoverOpenAICompatibleModels
      summary: Probe one configured endpoint's /models response with a bounded timeout.
      parameters:
        - { in: path, name: connectionId, required: true, schema: { type: string, pattern: '^oai_[a-f0-9]{12}$' } }
      responses:
        '200':
          description: Updated configuration with discovered IDs and safe health.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OpenAICompatibleConnectionConfig' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '502': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/runtimes/codex:
    get:
      operationId: getCodexRuntime
      summary: Read the install owner's redacted official-Codex runtime state.
      description: >
        Owner-only. Starts the isolated control-plane process lazily, then reads
        account state and provider-reported quota through Codex App Server. No
        account identity, token, auth-file content, login URL or device code is
        returned. Authentication does not guarantee a later model request.
      responses:
        '200':
          description: Redacted process, account, login-phase and quota projection.
          headers:
            Cache-Control: { schema: { type: string, const: no-store } }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CodexRuntimeStatus' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '503': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/runtimes/codex/login/start:
    post:
      operationId: startCodexLogin
      summary: Start one Codex-owned ChatGPT login.
      description: >
        Owner-only. Browser URLs and device codes are one-time response values;
        clients must not persist or log them. Codex owns token storage and refresh.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [kind]
              properties:
                kind: { type: string, enum: [browser, device] }
              additionalProperties: false
      responses:
        '200':
          description: One-time login presentation.
          headers:
            Cache-Control: { schema: { type: string, const: no-store } }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CodexLoginPresentation' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '503': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/runtimes/codex/login/wait:
    post:
      operationId: waitForCodexLogin
      summary: Bounded long-poll for the active Codex login attempt.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                timeoutMs: { type: integer, minimum: 0, maximum: 25000, default: 20000 }
              additionalProperties: false
      responses:
        '200':
          description: Current redacted runtime state after notification or timeout.
          headers:
            Cache-Control: { schema: { type: string, const: no-store } }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CodexRuntimeStatus' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '503': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/runtimes/codex/login/cancel:
    post:
      operationId: cancelCodexLogin
      summary: Cancel the one active Codex-owned login attempt.
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: false }
      responses:
        '200':
          description: Current redacted runtime state.
          headers:
            Cache-Control: { schema: { type: string, const: no-store } }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CodexRuntimeStatus' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/runtimes/codex/logout:
    post:
      operationId: logoutCodex
      summary: Ask Codex to revoke local managed auth and confirm signed-out state.
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: false }
      responses:
        '200':
          description: Confirmed redacted signed-out runtime state.
          headers:
            Cache-Control: { schema: { type: string, const: no-store } }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CodexRuntimeStatus' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '502': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/runtimes/codex/models:
    get:
      operationId: listCodexModels
      summary: List models observed from the exact Codex-managed ChatGPT account.
      description: >
        Owner-only. Refreshes account authority immediately before model/list.
        This catalogue is distinct from API-key and handwritten model catalogues.
      responses:
        '200':
          description: Provider-observed model catalogue with no account identity.
          headers:
            Cache-Control: { schema: { type: string, const: no-store } }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CodexModelCatalog' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '502': { $ref: '#/components/responses/ErrorResponse' }
        '503': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/sources:
    post:
      operationId: registerSource
      summary: Register one logical source using safe control metadata only.
      description: >
        Requires a delegated principal; workspace and profile scope come only
        from the verified bearer. The request cannot carry paths, URLs, bytes,
        storage locators, or workspace/profile authority. Equal metadata under
        distinct idempotency keys remains distinct logical sources.
      parameters:
        - in: header
          name: Idempotency-Key
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SourceRegistrationInput' }
      responses:
        '202':
          description: Pending safe manifest. No content or storage detail is returned.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SourceManifest' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/SourceCreationConflictResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }
        '500': { $ref: '#/components/responses/ErrorResponse' }
    get:
      operationId: listSources
      summary: List one bounded page of safe manifests in the verified scope.
      description: Requires a delegated principal. An empty scope returns an empty page.
      parameters:
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
        - in: query
          name: cursor
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Bounded source page without raw content or storage detail.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SourceList' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/sources/{sourceId}:
    get:
      operationId: getSource
      summary: Read one safe source manifest in the verified scope.
      description: Cross-workspace/profile identities are indistinguishable from absence.
      parameters:
        - in: path
          name: sourceId
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Safe source control manifest.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SourceManifest' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/sources/{sourceId}/deletions:
    post:
      operationId: createSourceDeletion
      summary: Freeze one exact source revision and enqueue verified deletion.
      description: >
        Requires the separate source_deletions.create authority and a UUID
        idempotency key. Workspace and profile scope come only from the delegated
        bearer. The response projects only safe progress counts and timestamps.
      parameters:
        - { in: path, name: sourceId, required: true, schema: { type: string, format: uuid } }
        - { in: header, name: Idempotency-Key, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [expectedRevision]
              properties:
                expectedRevision: { type: integer, minimum: 1 }
              additionalProperties: false
      responses:
        '202':
          description: Deletion durably accepted or exactly replayed; poll for verified terminal truth.
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceDeletion' } } }
        '400': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '401': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '403': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '404': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '409': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '413': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '429': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '500': { $ref: '#/components/responses/SourceDeletionErrorResponse' }

  /api/v1/source-deletions/{jobId}:
    get:
      operationId: getSourceDeletion
      summary: Read one scoped source-deletion projection or tombstone.
      description: >
        Requires source_deletions.read. Cross-workspace/profile identities are
        indistinguishable from absence. Artifact identities, paths, object keys,
        claims and raw failures are never returned.
      parameters:
        - { in: path, name: jobId, required: true, schema: { type: string, format: uuid } }
      responses:
        '200':
          description: Current safe deletion progress or verified minimal tombstone.
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceDeletion' } } }
        '400': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '401': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '403': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '404': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '429': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '500': { $ref: '#/components/responses/SourceDeletionErrorResponse' }

  /api/v1/source-deletions/{jobId}/cancel:
    post:
      operationId: cancelSourceDeletion
      summary: Request source-deletion cancellation before destruction begins.
      parameters:
        - { in: path, name: jobId, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: false }
      responses:
        '202':
          description: Cancellation requested or already requested; poll for terminal truth.
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceDeletionCancellationResult' } } }
        '400': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '401': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '403': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '404': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '409': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '413': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '429': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '500': { $ref: '#/components/responses/SourceDeletionErrorResponse' }

  /api/v1/source-deletions/{jobId}/retry:
    post:
      operationId: retrySourceDeletion
      summary: Requeue only the remaining inventory of a partial deletion.
      parameters:
        - { in: path, name: jobId, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: false }
      responses:
        '202':
          description: The same partial deletion inventory is queued for another verified pass.
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceDeletionRetryResult' } } }
        '400': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '401': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '403': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '404': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '409': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '413': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '429': { $ref: '#/components/responses/SourceDeletionErrorResponse' }
        '500': { $ref: '#/components/responses/SourceDeletionErrorResponse' }

  /api/v1/sources/{sourceId}/upload-sessions:
    post:
      operationId: createSourceUploadSession
      summary: Create a short-lived source-bound direct upload session.
      parameters:
        - { in: path, name: sourceId, required: true, schema: { type: string, format: uuid } }
        - { in: header, name: Idempotency-Key, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateSourceUploadSessionInput' }
      responses:
        '201':
          description: Bounded session metadata; no placement path or token is returned.
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceUploadSession' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/SourceCreationConflictResponse' }

  /api/v1/source-uploads/{uploadId}:
    patch:
      operationId: writeSourceUploadChunk
      summary: Stream one exact-offset verified chunk to runtime-private staging.
      parameters:
        - { in: path, name: uploadId, required: true, schema: { type: string, format: uuid } }
        - { in: header, name: Upload-Offset, required: true, schema: { type: integer, minimum: 0 } }
        - { in: header, name: Upload-Chunk-Checksum, required: true, schema: { type: string, pattern: '^sha256:[0-9a-f]{64}$' } }
      requestBody:
        required: true
        content:
          application/offset+octet-stream:
            schema: { type: string, contentMediaType: application/octet-stream, maxLength: 1048576 }
      responses:
        '200':
          description: Actual durable checkpoint; identical prior chunks replay without append.
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceUploadChunkResult' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/source-uploads/{uploadId}/complete:
    post:
      operationId: completeSourceUpload
      summary: Verify and atomically accept one immutable source version.
      description: >
        The upload session privately captures the source revision and current
        version at creation. Completion advances that exact identity once and
        resets current inspection and preparation state. A stale completion
        returns `source_upload_refresh_conflict` with the safe actual revision
        and current version identity; request bodies cannot override the fence.
      parameters:
        - { in: path, name: uploadId, required: true, schema: { type: string, format: uuid } }
      responses:
        '200':
          description: Idempotent replay of an already completed version.
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceUploadCompletionResult' } } }
        '201':
          description: Newly accepted immutable version.
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceUploadCompletionResult' } } }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '422': { $ref: '#/components/responses/ErrorResponse' }
        '500': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/sources/{sourceId}/versions/{sourceVersionId}:
    get:
      operationId: getSourceVersion
      summary: Read one immutable safe version manifest.
      parameters:
        - { in: path, name: sourceId, required: true, schema: { type: string, format: uuid } }
        - { in: path, name: sourceVersionId, required: true, schema: { type: string, format: uuid } }
      responses:
        '200':
          description: Version metadata without raw bytes or a storage locator.
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceVersionManifest' } } }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/sources/{sourceId}/versions/{sourceVersionId}/preparations:
    post:
      operationId: createSourcePreparation
      summary: Enqueue one allowlisted preparation for an inspected current source version.
      description: >
        Requires the separate source_preparations.create authority and a UUID
        idempotency key. The fixed extract_text and prepare_data_view operations
        are accepted; workspace and profile scope come only from the delegated
        bearer. Data View output cells and its private locator are never returned.
      parameters:
        - { in: path, name: sourceId, required: true, schema: { type: string, format: uuid } }
        - { in: path, name: sourceVersionId, required: true, schema: { type: string, format: uuid } }
        - { in: header, name: Idempotency-Key, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [operation]
              properties:
                operation: { type: string, enum: [extract_text, prepare_data_view] }
              additionalProperties: false
      responses:
        '202':
          description: Durable preparation job accepted; poll the unified job projection.
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceJob' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/SourceCreationConflictResponse' }
        '422': { $ref: '#/components/responses/ErrorResponse' }
        '500': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/sources/{sourceId}/versions/{sourceVersionId}/jobs:
    post:
      operationId: createSourceJob
      summary: Enqueue one allowlisted operation for an exact immutable source version.
      description: >
        Workspace and profile scope come only from the delegated bearer. Creation
        requires a UUID idempotency key; an exact completed retry replays the
        original safe job projection across Gateway restarts.
      parameters:
        - { in: path, name: sourceId, required: true, schema: { type: string, format: uuid } }
        - { in: path, name: sourceVersionId, required: true, schema: { type: string, format: uuid } }
        - { in: header, name: Idempotency-Key, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [operation]
              properties:
                operation: { type: string, const: inspect_format }
              additionalProperties: false
      responses:
        '202':
          description: Durable job accepted. The response contains no worker or storage detail.
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceJob' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/SourceCreationConflictResponse' }

  /api/v1/source-resources/{resourceId}:
    get:
      operationId: getSourceResource
      summary: Read one scoped content-free derived-resource manifest.
      description: >
        Requires the separate source_resources.read authority. Cross-workspace/profile
        identities are indistinguishable from absence. The response is lineage,
        policy, coverage and freshness metadata only; content requires a separate
        live grant and separately authorized operation.
      parameters:
        - { in: path, name: resourceId, required: true, schema: { type: string, format: uuid } }
      responses:
        '200':
          description: Closed safe resource manifest without content, paths, placement or diagnostics.
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceResourceManifest' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/source-data-views/{dataViewId}:
    get:
      operationId: getSourceDataView
      summary: Read one scoped content-free Data View manifest.
      description: >
        Requires the separate source_data_views.read authority. Cross-workspace/profile
        identities and views whose source is deleting are indistinguishable from absence.
        Field labels are untrusted source header labels; no cells, row values, private
        placement, diagnostics, grant, or query authority are returned.
      parameters:
        - { in: path, name: dataViewId, required: true, schema: { type: string, format: uuid } }
      responses:
        '200':
          description: Validated Data View lineage, policy, field shape, counts, and freshness only.
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceDataViewManifest' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/source-data-views/{dataViewId}/access-grants:
    post:
      operationId: createDataViewQueryGrant
      summary: Owner-only admission of one exact Data View observation fence.
      description: >
        Authentication must be enabled and the bearer must be the install owner.
        The request names explicit current field identities and one bounded row window;
        the Gateway atomically derives and stores the exact stable row identities. The
        fixed grant is source_data_view / source_data_views.query / observe. It grants
        no manifest, source-content, search, write, filter, sort, aggregation, SQL or
        expression authority. Exact idempotency retries return the same minimal receipt.
      parameters:
        - { in: path, name: dataViewId, required: true, schema: { type: string, format: uuid } }
        - { in: header, name: Idempotency-Key, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateDataViewQueryGrantInput' }
      responses:
        '201':
          description: Minimal immutable mutation receipt; no cells, row identities or policy detail.
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content: { application/json: { schema: { $ref: '#/components/schemas/AccessGrantMutationReceipt' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/source-data-views/{dataViewId}/query:
    post:
      operationId: querySourceDataView
      summary: Read one exact field list and bounded row window after live grant evaluation.
      description: >
        Requires a subject-bound delegated principal carrying source_data_views.query.
        Workspace, profile, subject, purpose and channel come only from verified signed
        claims; the body cannot override identity or authority. The live grant, strict
        current lineage/lifecycle floor and artifact integrity are checked before and after the
        bounded selection. Protected denial, stale/deleting state, tamper and race failures
        return no cells through one generic unavailable response. This is a retry-safe read
        POST with no durable side effect and no SQL, filter, sort, aggregation, expression
        or arbitrary row predicate.
      parameters:
        - { in: path, name: dataViewId, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/QuerySourceDataViewInput' }
      responses:
        '200':
          description: Verified current cells with complete immutable lineage and bounded-window truth.
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content: { application/json: { schema: { $ref: '#/components/schemas/ProtectedSourceDataViewSelection' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/source-resources/{resourceId}/access-grants:
    post:
      operationId: createAccessGrant
      summary: Owner-only creation of one bounded protected-source read or search grant.
      description: >
        Authentication must be enabled and the bearer must be the install owner.
        Workspace/profile scope and immutable source lineage are derived by the
        Gateway. An exact UUID idempotency retry returns only the same minimal receipt.
      parameters:
        - { in: path, name: resourceId, required: true, schema: { type: string, format: uuid } }
        - { in: header, name: Idempotency-Key, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateAccessGrantInput' }
      responses:
        '201':
          description: Minimal immutable mutation receipt; no subject, consent, policy, or storage detail.
          content: { application/json: { schema: { $ref: '#/components/schemas/AccessGrantMutationReceipt' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/source-resources/{resourceId}/content:
    post:
      operationId: readSourceContent
      summary: Read one bounded UTF-8 range after live grant and source-state evaluation.
      description: >
        Requires a delegated principal with source_content.read. Workspace, profile,
        subject, purpose and channel come only from that principal. Owner tokens cannot bypass
        the route or grant. Grant, lineage, availability, freshness and deletion truth
        are checked before and after the private byte read; denial is indistinguishable
        from absence.
      parameters:
        - { in: path, name: resourceId, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ReadSourceContentInput' }
      responses:
        '200':
          description: Verified current source bytes and safe immutable lineage.
          content: { application/json: { schema: { $ref: '#/components/schemas/ProtectedSourceContent' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/source-resources/{resourceId}/content/search:
    post:
      operationId: searchSourceContent
      summary: Search one current prepared UTF-8 resource after live grant evaluation.
      description: >
        Requires a delegated principal with source_content.search, which is a
        separate grant from source_content.read. The Gateway performs a bounded
        literal scan without a model, network access, SQL, shell, or durable index.
        Workspace, profile, subject, purpose and channel come only from that principal.
        Grant and exact current lineage are checked before and after the scan;
        timeout returns no partial matches and denial is indistinguishable from absence.
      parameters:
        - { in: path, name: resourceId, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SearchSourceContentInput' }
      responses:
        '200':
          description: Byte-ordered bounded passages with stable evidence identities.
          content: { application/json: { schema: { $ref: '#/components/schemas/ProtectedSourceSearchResult' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '504': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/access-grants:
    get:
      operationId: listAccessGrants
      summary: Owner-only bounded list of current grant revisions and lifecycle truth.
      parameters:
        - { in: query, name: limit, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
        - { in: query, name: cursor, schema: { type: string, format: uuid } }
      responses:
        '200':
          description: Current grant heads ordered by opaque identity.
          content: { application/json: { schema: { $ref: '#/components/schemas/AccessGrantList' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/access-grants/{grantId}:
    get:
      operationId: getAccessGrant
      summary: Owner-only read of one current immutable grant revision.
      parameters:
        - { in: path, name: grantId, required: true, schema: { type: string, format: uuid } }
      responses:
        '200':
          description: Current immutable grant revision.
          content: { application/json: { schema: { $ref: '#/components/schemas/AccessGrant' } } }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/access-grants/{grantId}/revoke:
    post:
      operationId: revokeAccessGrant
      summary: Owner-only revocation fenced by the exact current revision.
      parameters:
        - { in: path, name: grantId, required: true, schema: { type: string, format: uuid } }
        - { in: header, name: Idempotency-Key, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [expectedRevision]
              properties:
                expectedRevision: { type: integer, minimum: 1 }
              additionalProperties: false
      responses:
        '200':
          description: Minimal immutable revocation receipt.
          content: { application/json: { schema: { $ref: '#/components/schemas/AccessGrantMutationReceipt' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/source-jobs/{jobId}:
    get:
      operationId: getSourceJob
      summary: Read one scoped safe source-job projection.
      description: Cross-workspace/profile identities are indistinguishable from absence.
      parameters:
        - { in: path, name: jobId, required: true, schema: { type: string, format: uuid } }
      responses:
        '200':
          description: Current durable state without claims, leases, retry times, paths, content, or raw diagnostics.
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceJob' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/source-jobs/{jobId}/cancel:
    post:
      operationId: cancelSourceJob
      summary: Request cancellation without claiming that in-flight inspection stopped.
      parameters:
        - { in: path, name: jobId, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: false
        content:
          application/json:
            schema: { type: object, additionalProperties: false }
      responses:
        '202':
          description: Cancellation requested or already requested; poll the job for terminal truth.
          content: { application/json: { schema: { $ref: '#/components/schemas/SourceJobCancellationResult' } } }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/candidates/validate:
    post:
      operationId: validateCandidate
      summary: Validate bounded portable Agent Kit bytes without installing or activating them.
      description: >
        Accepts only direct base64 file bytes with portable relative names. Host paths,
        URLs and ambient filesystem references are not accepted. Validation occurs in
        an isolated temporary directory that is removed before a successful response.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [files]
              properties:
                files:
                  type: array
                  minItems: 1
                  maxItems: 1000
                  items:
                    type: object
                    required: [path, contentBase64]
                    properties:
                      path:
                        type: string
                        minLength: 1
                        maxLength: 256
                        description: Unique portable relative path; traversal, absolute paths and backslashes are rejected.
                      contentBase64:
                        type: string
                        contentEncoding: base64
                    additionalProperties: false
              additionalProperties: false
      responses:
        '200':
          description: Safe validation result. Invalid candidates return valid=false rather than being installed.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CandidateValidationResult' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }
        '500': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/candidates/stage:
    post:
      operationId: stageCandidate
      summary: Privately stage exact validated candidate bytes without activation.
      description: >
        Revalidates direct portable bytes, verifies the caller-supplied opaque
        identity after private placement, and records an explicit ready or
        failure state. It never changes the active profile. Repeating the same
        ready identity is idempotent after stored-byte verification.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [candidateId, files]
              properties:
                candidateId:
                  type: string
                  pattern: '^sha256:[0-9a-f]{64}$'
                files:
                  type: array
                  minItems: 1
                  maxItems: 1000
                  items:
                    type: object
                    required: [path, contentBase64]
                    properties:
                      path: { type: string, minLength: 1, maxLength: 256 }
                      contentBase64: { type: string, contentEncoding: base64 }
                    additionalProperties: false
              additionalProperties: false
      responses:
        '200':
          description: Exact durable stage state; ready=false is never activation success.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CandidateStageResult' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }
        '500': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/candidates/activate:
    post:
      operationId: activateCandidate
      summary: Atomically activate a ready candidate when the expected active identity matches.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [profileId, candidateId, expectedActiveCandidateId]
              properties:
                profileId: { type: string, minLength: 1, maxLength: 128 }
                candidateId: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
                expectedActiveCandidateId:
                  type: [string, 'null']
                  pattern: '^sha256:[0-9a-f]{64}$'
                  description: Null only when replacing the legacy/unversioned active profile.
              additionalProperties: false
      responses:
        '200':
          description: Actual active identity after the compare-and-set attempt.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CandidateActivationResult' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }
        '500': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/candidates/rollback:
    post:
      operationId: rollbackCandidate
      summary: Roll back to a named ready candidate under an exact compare-and-set fence.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [profileId, candidateId, expectedActiveCandidateId]
              properties:
                profileId: { type: string, minLength: 1, maxLength: 128 }
                candidateId: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
                expectedActiveCandidateId:
                  type: [string, 'null']
                  pattern: '^sha256:[0-9a-f]{64}$'
              additionalProperties: false
      responses:
        '200':
          description: Actual active identity after rollback, including named post-switch failure.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CandidateRollbackResult' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }
        '500': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/profiles/{profileId}/pause:
    post:
      operationId: pauseProfile
      summary: Atomically block new runs while reporting existing pinned runs truthfully.
      parameters:
        - in: path
          name: profileId
          required: true
          schema: { type: string, minLength: 1, maxLength: 128 }
        - in: header
          name: Idempotency-Key
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [expectedDeploymentRevision]
              properties:
                expectedDeploymentRevision: { type: integer, minimum: 1 }
              additionalProperties: false
      responses:
        '200':
          description: Durable paused routing state and actual draining run count.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProfileDeploymentResult' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/profiles/{profileId}/resume:
    post:
      operationId: resumeProfile
      summary: Reverify the active candidate before accepting new work again.
      parameters:
        - in: path
          name: profileId
          required: true
          schema: { type: string, minLength: 1, maxLength: 128 }
        - in: header
          name: Idempotency-Key
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [expectedDeploymentRevision]
              properties:
                expectedDeploymentRevision: { type: integer, minimum: 1 }
              additionalProperties: false
      responses:
        '200':
          description: Durable active routing state after exact-byte runtime verification.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProfileDeploymentResult' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/profiles:
    get:
      operationId: listProfiles
      summary: List the caller's bounded public profile catalog.
      responses:
        '200':
          description: Delegated callers receive only their scoped profile and safe deployment summary.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/PublicProfileSummary' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/profile-candidates/{candidateId}:
    parameters:
      - in: path
        name: candidateId
        required: true
        schema: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
    get:
      operationId: getCandidate
      summary: Read bounded candidate lifecycle and deletion eligibility.
      responses:
        '200':
          description: Safe candidate status without host paths or candidate bytes.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CandidateStatus' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
    delete:
      operationId: deleteCandidate
      summary: Delete only a verified eligible candidate using its natural idempotent identity.
      responses:
        '200':
          description: Actual verified deletion or explicit partial failure state.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CandidateDeletionResult' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/profiles/{profileId}/candidates:
    get:
      operationId: listCandidates
      summary: List bounded candidate status for one profile scope.
      parameters:
        - in: path
          name: profileId
          required: true
          schema: { type: string, minLength: 1, maxLength: 128 }
      responses:
        '200':
          description: Candidate status records in deterministic creation order.
          content:
            application/json:
              schema:
                type: object
                required: [profileId, items]
                properties:
                  profileId: { type: string }
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/CandidateStatus' }
                additionalProperties: false
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/profiles/{profileId}/deployment:
    get:
      operationId: getProfileDeployment
      summary: Read durable routing revision, actual active candidate and observed health.
      parameters:
        - in: path
          name: profileId
          required: true
          schema: { type: string, minLength: 1, maxLength: 128 }
      responses:
        '200':
          description: Safe current deployment status and active-run drain count.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProfileDeploymentStatus' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/auth/delegations:
    post:
      operationId: issueDelegation
      summary: Owner-only issuance of a short-lived scoped runtime principal.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [delegateId, workspaceId, profileId, purpose, operations]
              properties:
                delegateId: { type: string, minLength: 1, maxLength: 128 }
                subjectId:
                  type: string
                  minLength: 1
                  maxLength: 128
                  description: Explicit grant subject; required when operations includes source_content.read, source_content.search or source_data_views.query.
                workspaceId: { type: string, minLength: 1, maxLength: 128 }
                profileId: { type: string, minLength: 1, maxLength: 128 }
                purpose: { type: string, minLength: 1, maxLength: 64 }
                channel: { type: string, minLength: 1, maxLength: 64 }
                operations:
                  type: array
                  minItems: 1
                  maxItems: 32
                  items: { type: string }
                ttlSeconds: { type: integer, minimum: 1, maximum: 3600, default: 900 }
              additionalProperties: false
      responses:
        '201':
          description: Delegation issued. The token is a bearer secret and is never persisted by the Gateway.
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema:
                type: object
                required: [token, principal]
                properties:
                  token: { type: string, writeOnly: true }
                  principal: { $ref: '#/components/schemas/DelegatedPrincipal' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/auth/delegations/{tokenId}/revoke:
    post:
      operationId: revokeDelegation
      summary: Owner-only immediate revocation of a delegated runtime principal.
      parameters:
        - name: tokenId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason: { type: string, minLength: 1, maxLength: 64 }
              additionalProperties: false
      responses:
        '200':
          description: Delegation revoked.
          content:
            application/json:
              schema:
                type: object
                required: [tokenId, revoked]
                properties:
                  tokenId: { type: string, format: uuid }
                  revoked: { type: boolean, const: true }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/run:
    post:
      operationId: run
      summary: Start a run. Returns immediately; stream the reply over SSE.
      description: >
        A delegated principal may continue only a thread already bound to the
        same verified delegate, workspace, profile, subject, purpose and
        channel context. A mismatch or unbound thread is denied before mutation.
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          description: >
            UUID identifying this exact logical run start. Reuse only for a
            retry of the same validated input. Required for delegated principals.
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [profileId, prompt]
              properties:
                profileId:
                  type: string
                  description: Which profile answers (the `name` in its agent.json).
                prompt:
                  type: string
                threadId:
                  type: string
                  description: Continue an existing conversation; omit for a new thread.
                model:
                  type: string
                  description: >
                    Override the model (`provider:model`). Omitted → the
                    thread's model, else the install's `defaults.defaultModel`,
                    else the profile's default. Only an unavailable
                    same-runtime profile default may use the keyless fallback
                    (vault/env provider, else a reachable local Ollama).
                    Explicit request/thread/install choices fail honestly when
                    unavailable. A winner from any source that belongs to
                    another runtime fails instead of crossing that boundary.
                attachments:
                  type: array
                  maxItems: 8
                  description: >
                    Bounded one-turn untrusted data. This does not register a
                    reusable source or grant future access.
                  items:
                    $ref: '#/components/schemas/RunAttachmentInput'
      responses:
        '200':
          description: Run started in the background.
          content:
            application/json:
              schema:
                type: object
                required: [runId, threadId]
                properties:
                  runId: { type: string, format: uuid }
                  threadId: { type: string }
                  agentId: { type: string, description: "'root' for a plain run" }
                  profileId: { type: string }
                  candidateId:
                    type: [string, 'null']
                    pattern: '^sha256:[0-9a-f]{64}$'
                    description: Immutable candidate pinned before execution; null for a legacy profile.
                  model:
                    type: string
                    description: The model ACTUALLY dispatched (incl. keyless fallback).
                  modelSubstitution:
                    $ref: '#/components/schemas/ModelSubstitution'
                  status: { type: string }
                  timeoutMs:
                    type: integer
                    minimum: 1
                    description: Enforced wall-clock run timeout selected from the resolved profile.
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }
        '422': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/runs/{runId}:
    get:
      operationId: runSnapshot
      summary: Read one immutable run's bounded durable lifecycle snapshot.
      description: >
        Delegated access requires the run's thread to remain bound to the same
        verified authority context that created it.
      parameters:
        - name: runId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Safe execution snapshot; thread status and socket state are not substitutes.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RunSnapshot' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/runs/{runId}/events:
    get:
      operationId: runEvents
      summary: Replay and tail only one immutable run's root-agent event interval.
      description: >
        Delegated access requires the run's thread to remain bound to the same
        verified authority context that created it.
      parameters:
        - name: runId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: since
          in: query
          required: false
          schema: { type: integer, minimum: 0 }
          description: Exclusive cursor; defaults to the run's persisted startSeq.
      responses:
        '200':
          description: Bounded text/event-stream. Terminal snapshots close after replay.
          content:
            text/event-stream:
              schema: { type: string }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '410': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/threads/{threadId}/agents/root/events:
    get:
      operationId: events
      summary: >
        The run's event stream (SSE). Stays open indefinitely — a reply
        is finished at its terminal event (see asyncapi.yaml), not at
        stream close. Reconnect with ?since=<highest seq seen>.
      parameters:
        - name: threadId
          in: path
          required: true
          schema: { type: string }
        - name: since
          in: query
          required: false
          schema: { type: integer, default: 0 }
          description: Replay events with seq greater than this cursor.
      responses:
        '200':
          description: text/event-stream; each frame's data is one JSON event carrying `seq`.
          content:
            text/event-stream:
              schema: { type: string }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/threads/{threadId}/resume:
    post:
      operationId: resume
      summary: Answer a permission.request (or other pause) — the run resumes.
      parameters:
        - name: threadId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [action]
              properties:
                action:
                  type: string
                  enum: [approve, deny, always, answer, allow_folder_session]
                answer:
                  type: string
                  description: Free-text reply when action is `answer`.
                requestId:
                  type: string
                  description: Specific pending request when several are outstanding.
                grantPath:
                  type: string
                  description: Absolute path granted when action is `allow_folder_session`.
      responses:
        '200': { description: Decision delivered. }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/runs/{runId}/permissions/{requestId}/decision:
    post:
      operationId: decidePermission
      summary: Decide one exact permission request for one immutable run.
      parameters:
        - name: runId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: requestId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [decision, operationHash]
              additionalProperties: false
              properties:
                decision: { type: string, enum: [approve, deny] }
                operationHash: { type: string, pattern: '^[0-9a-f]{64}$' }
      responses:
        '200':
          description: Exact decision durably recorded and delivered to the live request.
          content:
            application/json:
              schema:
                type: object
                required: [runId, requestId, decision, operationHash]
                properties:
                  runId: { type: string, format: uuid }
                  requestId: { type: string }
                  decision: { type: string, enum: [approve, deny] }
                  operationHash: { type: string, pattern: '^[0-9a-f]{64}$' }
        '400': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }
        '413': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/runs/{runId}/cancel:
    post:
      operationId: cancelRun
      summary: Durably request cancellation of one immutable run.
      parameters:
        - name: runId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '202':
          description: Cancellation was durably requested; final outcome is not yet confirmed.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RunCancellationResult' }
        '200':
          description: The run was already terminal; no state was reopened.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RunCancellationResult' }
        '404': { $ref: '#/components/responses/ErrorResponse' }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '409': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/threads/{threadId}/abort:
    post:
      operationId: abortLegacy
      deprecated: true
      summary: Owner-only legacy thread abort; delegated clients use exact run cancellation.
      parameters:
        - name: threadId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200': { description: Legacy cancellation signal accepted; this is not proof of final cancellation. }
        '401': { $ref: '#/components/responses/ErrorResponse' }
        '403': { $ref: '#/components/responses/ErrorResponse' }
        '404': { $ref: '#/components/responses/ErrorResponse' }

  /api/v1/models:
    get:
      operationId: models
      deprecated: true
      summary: Legacy model-array compatibility view projected from Provider Hub.
      responses:
        '200':
          description: Curated compatibility rows sourced from the canonical Provider Hub generation.
          headers:
            Deprecation:
              schema: { type: string, enum: ['true'] }
              description: This compatibility route is deprecated.
            Link:
              schema: { type: string }
              description: Successor relation to `/api/v1/provider-hub/models`.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required: [id]
                  properties:
                    id: { type: string, description: "Canonical `provider:model` id." }
                    name: { type: string }
                    provider: { type: string }
                    hasCredentials:
                      type: boolean
                      description: >
                        Deprecated compatibility projection of Provider Hub
                        connection state for this route.
                    default:
                      type: boolean
                      description: At most one per provider — the recommended pick.
        '401': { $ref: '#/components/responses/ErrorResponse' }

components:
  responses:
    ErrorResponse:
      description: Safe typed error. Raw internal/provider detail is not returned.
      headers:
        X-Ownware-Correlation-Id:
          schema: { type: string, format: uuid }
          description: Opaque server-generated support correlation.
      content:
        application/json:
          schema: { $ref: '#/components/schemas/ErrorResponse' }
    SourceCreationConflictResponse:
      description: >
        Safe typed creation conflict. `source_quota_exceeded` reports only the
        blocked resource class plus standard safe error metadata; it never
        reports usage, failed workspace/profile scope, resource identities,
        paths, or a retry delay.
      headers:
        X-Ownware-Correlation-Id:
          schema: { type: string, format: uuid }
          description: Opaque server-generated support correlation.
      content:
        application/json:
          schema:
            oneOf:
              - allOf:
                  - { $ref: '#/components/schemas/ErrorResponse' }
                  - not:
                      required: [error]
                      properties:
                        error: { const: source_quota_exceeded }
              - { $ref: '#/components/schemas/SourceQuotaExceededError' }
    SourceDeletionErrorResponse:
      description: Safe typed deletion error without artifact or storage detail.
      headers:
        X-Ownware-Correlation-Id:
          schema: { type: string, format: uuid }
          description: Opaque server-generated support correlation.
        Retry-After:
          schema: { type: integer, minimum: 0 }
          description: Present for rate limits and an in-progress idempotent request only.
      content:
        application/json:
          schema: { $ref: '#/components/schemas/SourceDeletionError' }
  schemas:
    TaskPackScope:
      type: object
      required: [taskPackId, scopeKind, scopeId, decision, version, revision, updatedAt]
      properties:
        taskPackId: { type: string }
        scopeKind: { type: string, enum: [global, workspace, agent] }
        scopeId: { type: [string, 'null'] }
        decision: { type: string, enum: [allow, deny] }
        version: { type: [string, 'null'] }
        revision: { type: integer, minimum: 1 }
        updatedAt: { type: string, format: date-time }
      additionalProperties: false
    TaskPackTask:
      type: object
      required: [id, label, description, examples]
      properties:
        id: { type: string }
        label: { type: string }
        description: { type: string }
        examples: { type: array, items: { type: string } }
      additionalProperties: false
    TaskPackCatalogEntry:
      type: object
      required: [id, name, description, availableVersions, effectiveVersion, tasks, scopes]
      properties:
        id: { type: string }
        name: { type: string }
        description: { type: string }
        availableVersions: { type: array, items: { type: string } }
        effectiveVersion:
          type: [string, 'null']
          description: Exact selected version for the requested context, or null when disabled.
        tasks: { type: array, items: { $ref: '#/components/schemas/TaskPackTask' } }
        scopes: { type: array, items: { $ref: '#/components/schemas/TaskPackScope' } }
      additionalProperties: false
    TaskPackCatalog:
      type: object
      required: [taskPacks]
      properties:
        taskPacks:
          type: array
          items: { $ref: '#/components/schemas/TaskPackCatalogEntry' }
      additionalProperties: false
    SetTaskPackScopeInput:
      type: object
      required: [scopeKind, scopeId, decision, version, expectedRevision]
      properties:
        scopeKind: { type: string, enum: [global, workspace, agent] }
        scopeId: { type: [string, 'null'] }
        decision: { type: string, enum: [allow, deny] }
        version: { type: [string, 'null'] }
        expectedRevision: { type: [integer, 'null'], minimum: 1 }
      additionalProperties: false
    ProviderHubGeneration:
      type: object
      required: [id, schemaVersion, source, generatedAt, sha256]
      properties:
        id: { type: string }
        schemaVersion: { type: integer, const: 1 }
        source: { type: string, enum: [bundled, cache, network, manual] }
        sourceUrl: { type: string, format: uri }
        generatedAt: { type: string, format: date-time }
        fetchedAt: { type: string, format: date-time }
        upstreamVersion: { type: string }
        sha256: { type: string, pattern: '^[a-f0-9]{64}$' }
      additionalProperties: false
    ProviderHubCatalogHealth:
      type: object
      required: [status, activeGenerationId]
      properties:
        status: { type: string, enum: [fresh, stale, offline, error] }
        activeGenerationId: { type: string }
        lastAttemptAt: { type: string, format: date-time }
        lastSuccessAt: { type: string, format: date-time }
        nextRefreshAt: { type: string, format: date-time }
        errorCode: { type: string }
        errorMessage: { type: string }
      additionalProperties: false
    ProviderHubOverview:
      type: object
      required: [generation, generationId, catalogHealth, counts, warnings]
      properties:
        generation: { $ref: '#/components/schemas/ProviderHubGeneration' }
        generationId: { type: string }
        catalogHealth: { $ref: '#/components/schemas/ProviderHubCatalogHealth' }
        counts:
          type: object
          required: [providers, routes, models, pricebookEntries, connections]
          properties:
            providers: { type: integer, minimum: 0 }
            routes: { type: integer, minimum: 0 }
            models: { type: integer, minimum: 0 }
            pricebookEntries: { type: integer, minimum: 0 }
            connections: { type: integer, minimum: 0 }
          additionalProperties: false
        warnings: { type: array, items: { type: string } }
      additionalProperties: false
    ProviderHubFamily:
      type: object
      required: [id, name, lifecycle]
      properties:
        id: { type: string }
        name: { type: string }
        description: { type: string }
        websiteUrl: { type: string, format: uri }
        brandId: { type: string }
        lifecycle: { type: string, enum: [active, experimental, deprecated, hidden] }
      additionalProperties: false
    ProviderHubRoute:
      type: object
      required: [id, familyId, name, kind, transport, connectable, lifecycle]
      properties:
        id: { type: string }
        familyId: { type: string }
        name: { type: string }
        kind: { type: string, enum: [direct, router, cloud, subscription, local, custom, unknown] }
        transport:
          type: object
          required: [runtimeId, protocol]
          properties:
            runtimeId: { type: string }
            adapterId: { type: string }
            protocol: { type: string }
            sdkPackage: { type: string }
          additionalProperties: false
        apiBaseUrl: { type: string, format: uri }
        docsUrl: { type: string, format: uri }
        region: { type: string }
        plan: { type: string }
        connectable: { type: boolean }
        connectabilityReason: { type: string }
        lifecycle: { type: string, enum: [active, experimental, deprecated, hidden] }
        sourceRef: { type: string }
      additionalProperties: false
    ProviderHubProviderPage:
      type: object
      required: [generationId, items, warnings]
      properties:
        generationId: { type: string }
        items:
          type: array
          items:
            type: object
            required: [family, routes, modelCount, connectableModelCount, connectedModelCount, verifiedModelCount, recommendedModelCount]
            properties:
              family: { $ref: '#/components/schemas/ProviderHubFamily' }
              routes: { type: array, items: { $ref: '#/components/schemas/ProviderHubRoute' } }
              modelCount: { type: integer, minimum: 0 }
              connectableModelCount: { type: integer, minimum: 0 }
              connectedModelCount: { type: integer, minimum: 0 }
              verifiedModelCount: { type: integer, minimum: 0 }
              recommendedModelCount: { type: integer, minimum: 0 }
            additionalProperties: false
        warnings: { type: array, items: { type: string } }
      additionalProperties: false
    ProviderHubConnection:
      type: object
      required: [id, providerRouteId, label, auth, settings, status, createdAt, updatedAt]
      properties:
        id: { type: string }
        providerRouteId: { type: string }
        label: { type: string }
        auth:
          type: object
          description: Secret-free tagged authentication strategy; values are credential references only.
        settings:
          type: object
          description: Non-secret route settings.
        status: { type: string, enum: [configured, disabled, error] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        health: { $ref: '#/components/schemas/ProviderHubConnectionHealth' }
      additionalProperties: false
    ProviderHubConnectionHealth:
      type: object
      required: [status]
      properties:
        status: { type: string, enum: [unknown, healthy, degraded, failed] }
        checkedAt: { type: string, format: date-time }
        latencyMs: { type: integer, minimum: 0 }
        errorCode: { type: string }
        errorMessage: { type: string }
      additionalProperties: false
    ProviderHubConnectionPage:
      type: object
      required: [generationId, items, warnings]
      properties:
        generationId: { type: string }
        items: { type: array, items: { $ref: '#/components/schemas/ProviderHubConnection' } }
        warnings: { type: array, items: { type: string } }
      additionalProperties: false
    ProviderHubVerificationOverview:
      type: object
      required: [generationId, bundle, warnings]
      properties:
        generationId: { type: string }
        bundle:
          description: Independently versioned secret-free verification evidence, or null before a run.
          oneOf: [{ type: object }, { type: 'null' }]
        warnings: { type: array, items: { type: string } }
      additionalProperties: false
    ProviderHubModelRoute:
      type: object
      required: [id, providerRouteId, wireModelId, name, aliases, contextWindow, maxInputTokens, maxOutputTokens, capabilities, variants, availability, billingKind, catalogSourceRef]
      properties:
        id: { type: string }
        providerRouteId: { type: string }
        wireModelId: { type: string }
        name: { type: string }
        description: { type: string }
        modelFamily: { type: string }
        modelAuthor: { type: string }
        aliases: { type: array, items: { type: string } }
        releaseDate: { type: string, format: date }
        knowledgeCutoff: { type: string }
        contextWindow: { type: [integer, 'null'], minimum: 1 }
        maxInputTokens: { type: [integer, 'null'], minimum: 1 }
        maxOutputTokens: { type: [integer, 'null'], minimum: 1 }
        capabilities:
          type: array
          items:
            type: object
            required: [capability, upstream, ownware]
            properties:
              capability: { type: string }
              upstream: { type: object }
              ownware: { type: object }
            additionalProperties: false
        variants: { type: array, items: { type: object } }
        defaultVariantIds: { type: array, items: { type: string } }
        availability:
          type: object
          required: [catalogued, connectable, credentialed, verified, recommended, lifecycle, connectionIds]
          properties:
            catalogued: { type: boolean }
            connectable: { type: boolean }
            credentialed: { type: boolean }
            verified: { type: boolean }
            recommended: { type: boolean }
            lifecycle: { type: string, enum: [active, experimental, deprecated, hidden] }
            connectionIds: { type: array, items: { type: string } }
            reason: { type: string }
          additionalProperties: false
        billingKind: { type: string, enum: [metered, provider_reported, subscription, local, unknown] }
        catalogSourceRef: { type: string }
      additionalProperties: false
    ProviderHubPricebookEntry:
      type: object
      required: [id, version, currency, scope, rates, effectiveFrom, effectiveUntil, source]
      properties:
        id: { type: string }
        version: { type: string }
        currency: { type: string, const: USD }
        scope: { type: object }
        rates: { type: array, minItems: 1, items: { type: object } }
        effectiveFrom: { type: [string, 'null'], format: date-time }
        effectiveUntil: { type: [string, 'null'], format: date-time }
        source: { type: object }
      additionalProperties: false
    ProviderHubCostClassification:
      type: string
      enum: [estimated, provider_reported, reconciled, subscription, local, unknown]
    ProviderHubUsageCost:
      type: object
      required: [classification, amountUsd, currency, observedAt]
      properties:
        classification: { $ref: '#/components/schemas/ProviderHubCostClassification' }
        amountUsd: { type: [number, 'null'], minimum: 0 }
        currency: { type: string, const: USD }
        pricebookEntryId: { type: string }
        pricebookVersion: { type: string }
        observedAt: { type: string, format: date-time }
        reconciledAt: { type: string, format: date-time }
      additionalProperties: false
    ProviderHubReconciledCostInput:
      allOf:
        - { $ref: '#/components/schemas/ProviderHubUsageCost' }
        - type: object
          required: [classification, amountUsd, reconciledAt]
          properties:
            classification: { type: string, const: reconciled }
            amountUsd: { type: number, minimum: 0 }
            reconciledAt: { type: string, format: date-time }
    ProviderHubUsageEntry:
      type: object
      required: [id, occurredAt, providerFamilyId, providerRouteId, modelRouteId, wireModelId, billingKind, tokens, units, cost, providerFacts, success]
      properties:
        id: { type: string }
        occurredAt: { type: string, format: date-time }
        threadId: { type: string }
        profileId: { type: string }
        providerFamilyId: { type: string }
        providerRouteId: { type: string }
        modelRouteId: { type: string }
        connectionId: { type: string }
        wireModelId: { type: string }
        serviceTier: { type: string }
        contextTier: { type: string }
        region: { type: string }
        billingKind: { type: string, enum: [metered, provider_reported, subscription, local, unknown] }
        tokens:
          type: object
          properties:
            inputTextTokens: { type: integer, minimum: 0 }
            outputTextTokens: { type: integer, minimum: 0 }
            cacheReadTokens: { type: integer, minimum: 0 }
            cacheWriteTokens: { type: integer, minimum: 0 }
            reasoningTokens: { type: integer, minimum: 0 }
            inputAudioTokens: { type: integer, minimum: 0 }
            outputAudioTokens: { type: integer, minimum: 0 }
          additionalProperties: false
        units:
          type: object
          properties:
            inputImages: { type: integer, minimum: 0 }
            outputImages: { type: integer, minimum: 0 }
            requests: { type: integer, minimum: 0 }
            toolCalls: { type: integer, minimum: 0 }
          additionalProperties: false
        cost: { $ref: '#/components/schemas/ProviderHubUsageCost' }
        providerFacts:
          type: object
          properties:
            requestId: { type: string }
            generationId: { type: string }
            servedModelId: { type: string }
            servedProvider: { type: string }
            servedTier: { type: string }
            finishReason: { type: string }
            usagePayloadHash: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
          additionalProperties: false
        durationMs: { type: number, minimum: 0 }
        success: { type: boolean }
      additionalProperties: false
    ProviderHubUsagePage:
      type: object
      required: [items]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/ProviderHubUsageEntry' } }
      additionalProperties: false
    ProviderHubUsageFact:
      type: object
      required: [id, occurredAt, providerFamilyId, providerRouteId, modelRouteId, wireModelId, billingKind, tokens, units, providerFacts, success]
      properties:
        id: { type: string }
        occurredAt: { type: string, format: date-time }
        threadId: { type: string }
        profileId: { type: string }
        providerFamilyId: { type: string }
        providerRouteId: { type: string }
        modelRouteId: { type: string }
        connectionId: { type: string }
        wireModelId: { type: string }
        serviceTier: { type: string }
        contextTier: { type: string }
        region: { type: string }
        billingKind: { type: string, enum: [metered, provider_reported, subscription, local, unknown] }
        tokens: { type: object }
        units: { type: object }
        providerFacts: { type: object }
        durationMs: { type: number, minimum: 0 }
        success: { type: boolean }
      additionalProperties: false
    ProviderHubUsageSummary:
      type: object
      required: [observations, tokens]
      properties:
        observations:
          type: object
          required: [estimated, provider_reported, reconciled, subscription, local, unknown]
          additionalProperties:
            type: object
            required: [requestCount, amountUsd]
            properties:
              requestCount: { type: integer, minimum: 0 }
              amountUsd: { type: [number, 'null'], minimum: 0 }
            additionalProperties: false
        tokens:
          type: object
          required: [inputTextTokens, outputTextTokens, cacheReadTokens, cacheWriteTokens, reasoningTokens]
          properties:
            inputTextTokens: { type: integer, minimum: 0 }
            outputTextTokens: { type: integer, minimum: 0 }
            cacheReadTokens: { type: integer, minimum: 0 }
            cacheWriteTokens: { type: integer, minimum: 0 }
            reasoningTokens: { type: integer, minimum: 0 }
          additionalProperties: false
      additionalProperties: false
    ProviderHubUsageEvidenceExport:
      type: object
      required: [entries, pricebookSnapshots]
      properties:
        entries:
          type: array
          items:
            type: object
            required: [fact, recordedAt, costObservations]
            properties:
              fact:
                $ref: '#/components/schemas/ProviderHubUsageFact'
              recordedAt: { type: string, format: date-time }
              costObservations:
                type: array
                items:
                  type: object
                  required: [id, sequence, cost, recordedAt]
                  properties:
                    id: { type: string }
                    sequence: { type: integer, minimum: 1 }
                    cost: { $ref: '#/components/schemas/ProviderHubUsageCost' }
                    recordedAt: { type: string, format: date-time }
                  additionalProperties: false
            additionalProperties: false
        pricebookSnapshots:
          type: array
          items:
            type: object
            required: [entry, recordedAt]
            properties:
              entry: { $ref: '#/components/schemas/ProviderHubPricebookEntry' }
              recordedAt: { type: string, format: date-time }
            additionalProperties: false
      additionalProperties: false
    ProviderHubModelPage:
      type: object
      required: [generationId, items, page, warnings]
      properties:
        generationId: { type: string }
        items:
          type: array
          items:
            type: object
            required: [model, prices]
            properties:
              model: { $ref: '#/components/schemas/ProviderHubModelRoute' }
              prices: { type: array, items: { $ref: '#/components/schemas/ProviderHubPricebookEntry' } }
            additionalProperties: false
        page:
          type: object
          required: [limit, total, nextCursor]
          properties:
            limit: { type: integer, minimum: 1, maximum: 200 }
            total: { type: integer, minimum: 0 }
            nextCursor: { type: [string, 'null'] }
          additionalProperties: false
        warnings: { type: array, items: { type: string } }
      additionalProperties: false
    OpenAICompatibleConnectionInput:
      type: object
      required: [label, baseUrl, auth]
      properties:
        id: { type: string, pattern: '^oai_[a-f0-9]{12}$' }
        templateId: { type: string, enum: [lmstudio, ollama, azure-openai, amazon-bedrock-mantle] }
        label: { type: string, minLength: 1, maxLength: 200 }
        baseUrl: { type: string, minLength: 1, maxLength: 2000 }
        auth:
          oneOf:
            - type: object
              required: [kind]
              properties: { kind: { type: string, const: none } }
              additionalProperties: false
            - type: object
              required: [kind]
              properties:
                kind: { type: string, const: bearer }
                credentialId: { type: string }
                key: { type: string, minLength: 1, maxLength: 65536, writeOnly: true }
              additionalProperties: false
            - type: object
              required: [kind, name]
              properties:
                kind: { type: string, const: header }
                credentialId: { type: string }
                key: { type: string, minLength: 1, maxLength: 65536, writeOnly: true }
                name: { type: string }
                prefix: { type: string, maxLength: 100 }
              additionalProperties: false
        manualModelIds: { type: array, maxItems: 1000, items: { type: string } }
        discoveryEnabled: { type: boolean, default: true }
        compatibility: { $ref: '#/components/schemas/OpenAICompatibility' }
      additionalProperties: false
    OpenAICompatibility:
      type: object
      required: [maxTokensField, streamUsage]
      properties:
        maxTokensField: { type: string, enum: [max_tokens, max_completion_tokens] }
        streamUsage: { type: string, enum: [include, omit] }
      additionalProperties: false
    OpenAICompatibleConnectionConfig:
      type: object
      required: [schemaVersion, id, label, baseUrl, auth, manualModelIds, discoveredModelIds, discoveryEnabled, compatibility, health, createdAt, updatedAt]
      properties:
        schemaVersion: { type: integer, const: 1 }
        id: { type: string, pattern: '^oai_[a-f0-9]{12}$' }
        templateId: { type: string, enum: [lmstudio, ollama, azure-openai, amazon-bedrock-mantle] }
        label: { type: string }
        baseUrl: { type: string, format: uri }
        auth:
          type: object
          description: Secret-free auth configuration containing only credential references.
        manualModelIds: { type: array, items: { type: string } }
        discoveredModelIds: { type: array, items: { type: string } }
        discoveredAt: { type: string, format: date-time }
        discoveryEnabled: { type: boolean }
        compatibility: { $ref: '#/components/schemas/OpenAICompatibility' }
        health: { $ref: '#/components/schemas/ProviderHubConnectionHealth' }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
      additionalProperties: false
    OpenAICompatibleConnectionList:
      type: object
      required: [items]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/OpenAICompatibleConnectionConfig' } }
      additionalProperties: false
    CodexRuntimeStatus:
      type: object
      required: [runtime, account, login, quota]
      additionalProperties: false
      properties:
        runtime:
          type: object
          required: [id, accessRoute, support, upstreamSupport, processState, protocolVersion, supportedVersionRange]
          additionalProperties: false
          properties:
            id: { type: string, const: openai-codex }
            accessRoute: { type: string, const: openai-chatgpt-managed }
            support: { type: string, const: experimental }
            upstreamSupport: { type: string, const: experimental_unsupported_for_production }
            processState: { type: string, enum: [starting, running, closing, closed, failed] }
            protocolVersion: { type: string }
            supportedVersionRange: { type: string }
        account:
          oneOf:
            - type: object
              required: [state, reason]
              properties:
                state: { type: string, const: unknown }
                reason: { type: string, const: not_read }
              additionalProperties: false
            - type: object
              required: [state, requiresOpenaiAuth, authority, observedAt, validUntil]
              properties:
                state: { type: string, const: signed_out }
                requiresOpenaiAuth: { type: boolean }
                authority: { type: string, const: account/read }
                observedAt: { type: string, format: date-time }
                validUntil: { type: 'null' }
              additionalProperties: false
            - type: object
              required: [state, authMode, plan, requiresOpenaiAuth, authority, observedAt, validUntil]
              properties:
                state: { type: string, const: authenticated }
                authMode: { type: string, const: chatgpt }
                plan: { type: string }
                requiresOpenaiAuth: { type: boolean }
                authority: { type: string, const: account/read }
                observedAt: { type: string, format: date-time }
                validUntil: { type: 'null' }
              additionalProperties: false
            - type: object
              required: [state, authMode, requiresOpenaiAuth, authority, observedAt, validUntil]
              properties:
                state: { type: string, const: unsupported_auth }
                authMode: { type: string, enum: [apiKey, amazonBedrock] }
                requiresOpenaiAuth: { type: boolean }
                authority: { type: string, const: account/read }
                observedAt: { type: string, format: date-time }
                validUntil: { type: 'null' }
              additionalProperties: false
        login:
          type: object
          required: [phase]
          properties:
            phase: { type: string, enum: [idle, pending, cancelling, succeeded, cancelled, failed] }
            reason: { type: string, enum: [provider_rejected] }
          additionalProperties: false
        quota: { $ref: '#/components/schemas/CodexQuotaState' }

    CodexQuotaState:
      oneOf:
        - type: object
          required: [state, reason, validUntil]
          properties:
            state: { type: string, const: unknown }
            reason: { type: string, enum: [not_read, provider_stated_no_usable_limit, unrecognized_provider_shape] }
            authority: { type: string, enum: [account/rateLimits/read, account/rateLimits/updated] }
            observedAt: { type: string, format: date-time }
            validUntil: { type: 'null' }
          additionalProperties: false
        - type: object
          required: [state, authority, observedAt, validUntil, buckets, resetCreditsAvailable, resetCreditDetailsKnown]
          properties:
            state: { type: string, enum: [reported, exhausted] }
            authority: { type: string, enum: [account/rateLimits/read, account/rateLimits/updated] }
            observedAt: { type: string, format: date-time }
            validUntil: { type: 'null' }
            buckets:
              type: array
              items:
                type: object
                additionalProperties: false
                required: [id, name, plan, primary, secondary, reachedType, spendControlReached, hasCredits, unlimitedCredits, spendRemainingPercent, spendResetsAt]
                properties:
                  id: { type: [string, 'null'] }
                  name: { type: [string, 'null'] }
                  plan: { type: [string, 'null'] }
                  primary: { $ref: '#/components/schemas/CodexRateLimitWindow' }
                  secondary: { $ref: '#/components/schemas/CodexRateLimitWindow' }
                  reachedType: { type: [string, 'null'] }
                  spendControlReached: { type: [boolean, 'null'] }
                  hasCredits: { type: [boolean, 'null'] }
                  unlimitedCredits: { type: [boolean, 'null'] }
                  spendRemainingPercent: { type: [integer, 'null'], minimum: 0 }
                  spendResetsAt: { type: [string, 'null'], format: date-time }
            resetCreditsAvailable: { type: [integer, 'null'], minimum: 0 }
            resetCreditDetailsKnown: { type: [boolean, 'null'] }
          additionalProperties: false

    CodexRateLimitWindow:
      oneOf:
        - { type: 'null' }
        - type: object
          required: [usedPercent, windowDurationMinutes, resetsAt]
          additionalProperties: false
          properties:
            usedPercent: { type: integer, minimum: 0 }
            windowDurationMinutes: { type: [integer, 'null'], minimum: 0 }
            resetsAt: { type: [string, 'null'], format: date-time }

    CodexLoginPresentation:
      oneOf:
        - type: object
          required: [kind, loginId, url]
          properties:
            kind: { type: string, const: browser }
            loginId: { type: string }
            url: { type: string, format: uri, pattern: '^https:' }
          additionalProperties: false
        - type: object
          required: [kind, loginId, verificationUrl, userCode]
          properties:
            kind: { type: string, const: device }
            loginId: { type: string }
            verificationUrl: { type: string, format: uri, pattern: '^https:' }
            userCode: { type: string }
          additionalProperties: false

    CodexModelCatalog:
      type: object
      required: [authority, observedAt, validUntil, models]
      additionalProperties: false
      properties:
        authority: { type: string, const: model/list }
        observedAt: { type: string, format: date-time }
        validUntil: { type: 'null' }
        models:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [id, model, displayName, description, hidden, isDefault, defaultReasoningEffort, reasoningEfforts, inputModalities, serviceTiers, defaultServiceTier, supportsPersonality]
            properties:
              id: { type: string }
              model: { type: string }
              displayName: { type: string }
              description: { type: string }
              hidden: { type: boolean }
              isDefault: { type: boolean }
              defaultReasoningEffort: { type: string }
              reasoningEfforts: { type: array, items: { type: string } }
              inputModalities: { type: array, items: { type: string } }
              serviceTiers: { type: array, items: { type: string } }
              defaultServiceTier: { type: [string, 'null'] }
              supportsPersonality: { type: boolean }

    ModelSubstitution:
      type: object
      additionalProperties: false
      required: [configuredModel, effectiveModel, configuredSource, reason]
      description: >
        Gateway dispatch receipt present only when the normalized configured
        winner differs from the model actually dispatched. It does not prove
        provider acceptance, completion, capability support or billing.
      properties:
        configuredModel: { type: string, minLength: 1, maxLength: 256 }
        effectiveModel: { type: string, minLength: 1, maxLength: 256 }
        configuredSource:
          type: string
          const: profile
        reason:
          type: string
          enum: [profile_default_unavailable]

    RunSnapshot:
      type: object
      required: [runId, threadId, workspaceId, profileId, candidateId, model, timeoutMs, status, terminal, outcomeKnown, acceptedAt, startedAt, updatedAt, terminalAt, cancelRequestedAt, startSeq, endSeq, earliestRetainedCursor, code]
      properties:
        runId: { type: string, format: uuid }
        threadId: { type: string }
        workspaceId: { type: [string, 'null'] }
        profileId: { type: string }
        candidateId: { type: [string, 'null'], pattern: '^sha256:[0-9a-f]{64}$' }
        model: { type: string }
        timeoutMs: { type: integer, minimum: 1 }
        status:
          type: string
          enum: [accepted, running, waiting, cancel_requested, succeeded, failed, cancelled, timed_out, indeterminate]
        terminal: { type: boolean }
        outcomeKnown: { type: boolean }
        acceptedAt: { type: integer }
        startedAt: { type: [integer, 'null'] }
        updatedAt: { type: integer }
        terminalAt: { type: [integer, 'null'] }
        cancelRequestedAt: { type: [integer, 'null'] }
        startSeq: { type: integer, minimum: 0 }
        endSeq: { type: [integer, 'null'], minimum: 0 }
        earliestRetainedCursor:
          type: [integer, 'null']
          minimum: 0
          description: Earliest exclusive cursor that can replay this run without a retained-history gap.
        code: { type: [string, 'null'] }
    RunCancellationResult:
      type: object
      required: [runId, status, terminal, outcomeKnown, cancellation]
      properties:
        runId: { type: string, format: uuid }
        status:
          type: string
          enum: [accepted, running, waiting, cancel_requested, succeeded, failed, cancelled, timed_out, indeterminate]
        terminal: { type: boolean }
        outcomeKnown: { type: boolean }
        cancellation:
          type: string
          enum: [requested, already_requested, already_terminal]
    RunAttachmentInput:
      type: object
      required: [filename, data, mimeType]
      properties:
        filename:
          type: string
          minLength: 1
          maxLength: 255
          description: Safe display basename; path separators and control characters reject.
        data:
          type: string
          minLength: 4
          maxLength: 5592408
          contentEncoding: base64
          description: Strict canonical base64; decoded bytes are bounded independently.
        mimeType:
          type: string
          minLength: 3
          maxLength: 127
          description: Declared type; the runtime verifies it against bytes and filename category.
      additionalProperties: false
    PublicGatewayLimits:
      type: object
      required: [jsonBodyBytes, candidateUpload, runAttachments, sourceList, connectionList, sourceUpload, sourceInspection, sourcePreparation, sourceDataView, accessGrants, sourceContent, sourceSearch, sourceQuota, delegationDefaultTtlSeconds, delegationMaxTtlSeconds, idempotencyRetentionSeconds, rateLimit]
      properties:
        jsonBodyBytes:
          type: integer
          const: 10485760
          description: Maximum body size for public operations that parse JSON.
        candidateUpload:
          type: object
          required: [maxFiles, maxDecodedBytes, maxPathCharacters]
          properties:
            maxFiles: { type: integer, const: 1000 }
            maxDecodedBytes: { type: integer, const: 6291456 }
            maxPathCharacters: { type: integer, const: 256 }
          additionalProperties: false
        runAttachments:
          type: object
          required: [maxCount, maxItemDecodedBytes, maxTotalDecodedBytes, maxFilenameCharacters]
          properties:
            maxCount: { type: integer, const: 8 }
            maxItemDecodedBytes: { type: integer, const: 4194304 }
            maxTotalDecodedBytes: { type: integer, const: 6291456 }
            maxFilenameCharacters: { type: integer, const: 255 }
          additionalProperties: false
        sourceList:
          type: object
          required: [maxPageSize]
          properties:
            maxPageSize: { type: integer, const: 100 }
          additionalProperties: false
        connectionList:
          type: object
          required: [maxPageSize]
          properties:
            maxPageSize: { type: integer, const: 100 }
          additionalProperties: false
        sourceUpload:
          type: object
          required: [maxDecodedBytes, maxChunkBytes, maxChunks, sessionTtlSeconds, supportedSourceKinds, supportedMediaTypes]
          properties:
            maxDecodedBytes: { type: integer, const: 16777216 }
            maxChunkBytes: { type: integer, const: 1048576 }
            maxChunks: { type: integer, const: 64 }
            sessionTtlSeconds: { type: integer, const: 900 }
            supportedSourceKinds:
              type: array
              prefixItems:
                - { const: file }
                - { const: text }
                - { const: structured_export }
              minItems: 3
              maxItems: 3
            supportedMediaTypes:
              type: array
              prefixItems:
                - { const: text/plain }
                - { const: application/pdf }
                - { const: application/vnd.openxmlformats-officedocument.wordprocessingml.document }
                - { const: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet }
              minItems: 4
              maxItems: 4
          additionalProperties: false
        sourceInspection:
          type: object
          required: [maxBytes, perAttemptTimeoutMs, maxAttempts]
          properties:
            maxBytes: { type: integer, const: 16777216 }
            perAttemptTimeoutMs: { type: integer, const: 5000 }
            maxAttempts: { type: integer, const: 3 }
          additionalProperties: false
        sourcePreparation:
          type: object
          required: [maxBytes, perAttemptTimeoutMs, maxAttempts, maxResourcesPerJob]
          properties:
            maxBytes: { type: integer, const: 16777216 }
            perAttemptTimeoutMs: { type: integer, const: 5000 }
            maxAttempts: { type: integer, const: 3 }
            maxResourcesPerJob: { type: integer, const: 1 }
          additionalProperties: false
        sourceDataView:
          type: object
          required: [supportedFormats, maxSourceBytes, maxArtifactBytes, maxFields, maxRows, maxCellBytes, maxCells, perAttemptTimeoutMs, maxAttempts, maxQueryFields, maxQueryRows, maxQueryCells, maxQueryResultBytes, queryTimeoutMs, maxGrantScopeIds]
          properties:
            supportedFormats:
              type: array
              prefixItems:
                - { const: strict_utf8_csv }
              minItems: 1
              maxItems: 1
            maxSourceBytes: { type: integer, const: 16777216 }
            maxArtifactBytes: { type: integer, const: 134217728 }
            maxFields: { type: integer, const: 256 }
            maxRows: { type: integer, const: 100000 }
            maxCellBytes: { type: integer, const: 65536 }
            maxCells: { type: integer, const: 1000000 }
            perAttemptTimeoutMs: { type: integer, const: 5000 }
            maxAttempts: { type: integer, const: 3 }
            maxQueryFields: { type: integer, const: 32 }
            maxQueryRows: { type: integer, const: 256 }
            maxQueryCells: { type: integer, const: 8192 }
            maxQueryResultBytes: { type: integer, const: 262144 }
            queryTimeoutMs: { type: integer, const: 2000 }
            maxGrantScopeIds: { type: integer, const: 256 }
          additionalProperties: false
        accessGrants:
          type: object
          required: [minTtlSeconds, maxTtlSeconds, maxActivePerWorkspaceProfile, maxPageSize]
          properties:
            minTtlSeconds: { type: integer, const: 60 }
            maxTtlSeconds: { type: integer, const: 2592000 }
            maxActivePerWorkspaceProfile: { type: integer, const: 1024 }
            maxPageSize: { type: integer, const: 100 }
          additionalProperties: false
        sourceContent:
          type: object
          required: [maxRangeBytes]
          properties:
            maxRangeBytes: { type: integer, const: 65536 }
          additionalProperties: false
        sourceSearch:
          type: object
          required: [maxScanBytes, maxQueryBytes, maxMatches, maxContextBytes, perRequestTimeoutMs, matchModes]
          properties:
            maxScanBytes: { type: integer, const: 16777216 }
            maxQueryBytes: { type: integer, const: 128 }
            maxMatches: { type: integer, const: 20 }
            maxContextBytes: { type: integer, const: 1024 }
            perRequestTimeoutMs: { type: integer, const: 5000 }
            matchModes:
              type: array
              prefixItems:
                - { const: exact_utf8 }
                - { const: ascii_case_insensitive }
              minItems: 2
              maxItems: 2
          additionalProperties: false
        sourceQuota:
          type: object
          required: [workspace, profile]
          description: >
            Effective source ceilings negotiated for this Gateway instance.
            These values are instance limits, not schema-defined defaults.
          properties:
            workspace: { $ref: '#/components/schemas/SourceQuotaCeilings' }
            profile: { $ref: '#/components/schemas/SourceQuotaCeilings' }
          additionalProperties: false
        delegationDefaultTtlSeconds: { type: integer, const: 900 }
        delegationMaxTtlSeconds: { type: integer, const: 3600 }
        idempotencyRetentionSeconds: { type: integer, const: 604800 }
        rateLimit:
          type: object
          required: [enabled, windowSeconds, generalRequests, runStarts]
          properties:
            enabled: { type: boolean }
            windowSeconds: { type: integer, const: 60 }
            generalRequests: { type: integer, minimum: 0 }
            runStarts: { type: integer, minimum: 0 }
    SourceQuotaCeilings:
      type: object
      required: [maxSourceRegistrations, maxRetainedAndReservedBytes, maxActiveUploadSessions, maxNonterminalJobs, maxDerivedResources]
      description: Positive effective ceilings for one workspace or profile scope.
      properties:
        maxSourceRegistrations: { type: integer, minimum: 1 }
        maxRetainedAndReservedBytes: { type: integer, minimum: 1 }
        maxActiveUploadSessions: { type: integer, minimum: 1 }
        maxNonterminalJobs: { type: integer, minimum: 1 }
        maxDerivedResources: { type: integer, minimum: 1 }
      additionalProperties: false
    SourceRegistrationInput:
      type: object
      required: [kind, label, classification, authority, audiencePolicyRef, sensitivityPolicyRef, purposePolicyRef, retentionPolicyRef, freshnessPolicyRef]
      properties:
        kind:
          type: string
          enum: [file, text, visual, structured_export, cloud_document, connected_snapshot, supported_other]
        label:
          type: string
          minLength: 1
          maxLength: 160
          description: Safe single-line display label; control characters reject.
        classification: { type: string, enum: [public, internal, confidential, restricted] }
        authority: { type: string, enum: [source_of_record, supporting_reference, example, excluded] }
        audiencePolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        sensitivityPolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        purposePolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        retentionPolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        freshnessPolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
      additionalProperties: false
    SourceHealth:
      type: object
      required: [registration, inspection, preparation, access, freshness, conflict, deletion]
      properties:
        registration: { type: string, enum: [pending, registered, rejected] }
        inspection: { type: string, enum: [not_started, queued, inspecting, complete, partial, failed] }
        preparation: { type: string, enum: [not_requested, queued, preparing, ready, partial, failed] }
        access: { type: string, enum: [available, denied, expired, disconnected, wrong_identity] }
        freshness: { type: string, enum: [fresh, aging, stale, unknown] }
        conflict: { type: string, enum: [none, suspected, confirmed, resolved] }
        deletion: { type: string, enum: [active, frozen, deleting, partially_deleted, deleted] }
      additionalProperties: false
    SourceManifest:
      type: object
      required: [sourceId, kind, label, classification, authority, audiencePolicyRef, sensitivityPolicyRef, purposePolicyRef, retentionPolicyRef, freshnessPolicyRef, revision, currentVersionId, health, createdAt, updatedAt]
      properties:
        sourceId: { type: string, format: uuid }
        kind:
          type: string
          enum: [file, text, visual, structured_export, cloud_document, connected_snapshot, supported_other]
        label: { type: string, minLength: 1, maxLength: 160 }
        classification: { type: string, enum: [public, internal, confidential, restricted] }
        authority: { type: string, enum: [source_of_record, supporting_reference, example, excluded] }
        audiencePolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        sensitivityPolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        purposePolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        retentionPolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        freshnessPolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        revision: { type: integer, minimum: 1 }
        currentVersionId: { type: [string, 'null'], format: uuid }
        health: { $ref: '#/components/schemas/SourceHealth' }
        createdAt: { type: integer, minimum: 0 }
        updatedAt: { type: integer, minimum: 0 }
      additionalProperties: false
    SourceList:
      type: object
      required: [items, nextCursor]
      properties:
        items:
          type: array
          maxItems: 100
          items: { $ref: '#/components/schemas/SourceManifest' }
        nextCursor: { type: [string, 'null'], format: uuid }
      additionalProperties: false
    ConnectionInventoryItem:
      type: object
      required: [connectionId, capabilityId, status, recovery, changedAt, expiresAt, lastVerifiedAt]
      properties:
        connectionId:
          type: string
          format: uuid
          description: Ownware-owned opaque identity; never a vendor account handle.
        capabilityId:
          type: string
          pattern: '^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$'
          description: Provider-neutral logical capability identity.
        status: { type: string, enum: [pending, connected, failed, expired] }
        recovery:
          type: string
          enum: [none, complete_connection, reconnect, verify_revocation]
        changedAt:
          type: integer
          minimum: 0
          multipleOf: 1000
          description: Durable state-change time reduced to whole-second precision.
        expiresAt: { type: [integer, 'null'], minimum: 0, multipleOf: 1000 }
        lastVerifiedAt: { type: [integer, 'null'], minimum: 0, multipleOf: 1000 }
      additionalProperties: false
    ConnectionList:
      type: object
      required: [items, nextCursor, accessPolicy]
      properties:
        items:
          type: array
          maxItems: 100
          items: { $ref: '#/components/schemas/ConnectionInventoryItem' }
        nextCursor: { type: [string, 'null'], format: uuid }
        accessPolicy:
          type: string
          const: separate_grant_required
          description: Connection alone grants no resource or action authority.
      additionalProperties: false
    CreateSourceUploadSessionInput:
      type: object
      required: [expectedBytes, expectedChecksum, declaredMediaType, filename]
      properties:
        expectedBytes: { type: integer, minimum: 1, maximum: 16777216 }
        expectedChecksum: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        declaredMediaType: { type: string, enum: [text/plain, application/pdf, application/vnd.openxmlformats-officedocument.wordprocessingml.document, application/vnd.openxmlformats-officedocument.spreadsheetml.sheet] }
        filename: { type: string, minLength: 1, maxLength: 255, description: Untrusted basename label only. }
      additionalProperties: false
    SourceUploadSession:
      type: object
      required: [uploadId, sourceId, state, offset, expectedBytes, expectedChecksum, declaredMediaType, maxChunkBytes, maxChunks, expiresAt, createdAt]
      properties:
        uploadId: { type: string, format: uuid }
        sourceId: { type: string, format: uuid }
        state: { const: open }
        offset: { type: integer, const: 0 }
        expectedBytes: { type: integer, minimum: 1, maximum: 16777216 }
        expectedChecksum: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        declaredMediaType: { type: string, enum: [text/plain, application/pdf, application/vnd.openxmlformats-officedocument.wordprocessingml.document, application/vnd.openxmlformats-officedocument.spreadsheetml.sheet] }
        maxChunkBytes: { type: integer, const: 1048576 }
        maxChunks: { type: integer, const: 64 }
        expiresAt: { type: integer, minimum: 0 }
        createdAt: { type: integer, minimum: 0 }
      additionalProperties: false
    SourceUploadChunkResult:
      type: object
      required: [uploadId, state, offset, chunkCount, replayed]
      properties:
        uploadId: { type: string, format: uuid }
        state: { const: open }
        offset: { type: integer, minimum: 0 }
        chunkCount: { type: integer, minimum: 1, maximum: 64 }
        replayed: { type: boolean }
      additionalProperties: false
    SourceVersionManifest:
      type: object
      required: [sourceVersionId, sourceId, checksum, verifiedMediaType, byteCount, inspection, createdAt]
      properties:
        sourceVersionId: { type: string, format: uuid }
        sourceId: { type: string, format: uuid }
        checksum: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        verifiedMediaType: { type: string, enum: [text/plain, application/pdf, application/vnd.openxmlformats-officedocument.wordprocessingml.document, application/vnd.openxmlformats-officedocument.spreadsheetml.sheet] }
        byteCount: { type: integer, minimum: 1, maximum: 16777216 }
        inspection: { type: string, enum: [not_started, queued, inspecting, complete, partial, failed] }
        createdAt: { type: integer, minimum: 0 }
      additionalProperties: false
    SourceUploadCompletionResult:
      type: object
      required: [sourceVersionId, sourceId, checksum, verifiedMediaType, byteCount, inspection, createdAt, replayed]
      properties:
        sourceVersionId: { type: string, format: uuid }
        sourceId: { type: string, format: uuid }
        checksum: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        verifiedMediaType: { type: string, enum: [text/plain, application/pdf, application/vnd.openxmlformats-officedocument.wordprocessingml.document, application/vnd.openxmlformats-officedocument.spreadsheetml.sheet] }
        byteCount: { type: integer, minimum: 1, maximum: 16777216 }
        inspection: { const: not_started }
        createdAt: { type: integer, minimum: 0 }
        replayed: { type: boolean }
      additionalProperties: false
    SourceJob:
      type: object
      required: [jobId, sourceId, sourceVersionId, operation, implementationVersion, resourceId, dataViewId, state, attempt, maxAttempts, checkpoint, cancelRequestedAt, outcomeCode, createdAt, updatedAt, terminalAt]
      properties:
        jobId: { type: string, format: uuid }
        sourceId: { type: string, format: uuid }
        sourceVersionId: { type: string, format: uuid }
        operation: { type: string, enum: [inspect_format, extract_text, prepare_data_view] }
        implementationVersion: { type: string, enum: [inspect_format.v1, text_extraction.v1, csv_data_view.v1] }
        resourceId:
          type: [string, 'null']
          format: uuid
          description: Published only after successful preparation creates its safe resource manifest.
        dataViewId:
          type: [string, 'null']
          format: uuid
          description: Published only after successful Data View preparation; no cells or private locator are returned.
        state:
          type: string
          enum: [queued, running, waiting_for_resource, cancel_requested, succeeded, partial, failed, cancelled]
        attempt: { type: integer, minimum: 0, maximum: 3 }
        maxAttempts: { type: integer, const: 3 }
        checkpoint: { type: integer, minimum: 0, maximum: 4 }
        cancelRequestedAt: { type: [integer, 'null'], minimum: 0 }
        outcomeCode:
          type: [string, 'null']
          enum: [attempts_exhausted, cancelled, inspection_complete, inspection_timeout, inspection_unavailable, source_format_invalid, source_object_mismatch, source_object_missing, source_object_oversized, source_storage_inconsistent, preparation_complete, preparation_timeout, preparation_unavailable, data_view_unavailable, data_view_invalid, data_view_too_large, data_view_publication_conflict, artifact_cleanup_failed, csv_identity_invalid, csv_input_oversized, csv_invalid_utf8, csv_header_empty, csv_header_duplicate, csv_quoting_invalid, csv_bare_carriage_return, csv_row_ragged, csv_field_limit_exceeded, csv_row_limit_exceeded, csv_cell_limit_exceeded, csv_total_cell_limit_exceeded, csv_preparation_timeout, null]
        createdAt: { type: integer, minimum: 0 }
        updatedAt: { type: integer, minimum: 0 }
        terminalAt: { type: [integer, 'null'], minimum: 0 }
        cancellation: { type: string, enum: [requested, already_requested] }
      additionalProperties: false
    SourceResourceManifest:
      type: object
      required: [resourceId, jobId, sourceId, sourceVersionId, kind, operation, implementationVersion, sourceRevision, sourceChecksum, resourceChecksum, byteStart, byteEnd, byteCount, classification, authority, audiencePolicyRef, sensitivityPolicyRef, purposePolicyRef, retentionPolicyRef, freshnessPolicyRef, coverage, freshness, createdAt, staleAt]
      properties:
        resourceId: { type: string, format: uuid }
        jobId: { type: string, format: uuid }
        sourceId: { type: string, format: uuid }
        sourceVersionId: { type: string, format: uuid }
        kind: { type: string, const: text_extraction }
        operation: { type: string, const: extract_text }
        implementationVersion: { type: string, const: text_extraction.v1 }
        sourceRevision: { type: integer, minimum: 1 }
        sourceChecksum: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        resourceChecksum: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        byteStart: { type: integer, const: 0 }
        byteEnd: { type: integer, minimum: 1, maximum: 16777216 }
        byteCount: { type: integer, minimum: 1, maximum: 16777216 }
        classification: { type: string, enum: [public, internal, confidential, restricted] }
        authority: { type: string, enum: [source_of_record, supporting_reference, example] }
        audiencePolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        sensitivityPolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        purposePolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        retentionPolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        freshnessPolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        coverage: { type: string, const: complete }
        freshness: { type: string, enum: [current, stale] }
        createdAt: { type: integer, minimum: 0 }
        staleAt: { type: [integer, 'null'], minimum: 0 }
      additionalProperties: false
    SourceDataViewField:
      type: object
      required: [fieldId, ordinal, label]
      properties:
        fieldId: { type: string, pattern: '^field\.[0-9a-f]{32}$' }
        ordinal: { type: integer, minimum: 0, maximum: 255 }
        label:
          type: string
          minLength: 1
          maxLength: 65536
          description: Untrusted display label copied mechanically from the source header.
      additionalProperties: false
    SourceDataViewManifest:
      type: object
      required: [dataViewId, jobId, sourceId, sourceVersionId, implementationVersion, sourceRevision, sourceChecksum, artifactChecksum, artifactByteCount, fieldCount, rowCount, fields, classification, authority, audiencePolicyRef, sensitivityPolicyRef, purposePolicyRef, retentionPolicyRef, freshnessPolicyRef, freshness, createdAt, staleAt]
      properties:
        dataViewId: { type: string, format: uuid }
        jobId: { type: string, format: uuid }
        sourceId: { type: string, format: uuid }
        sourceVersionId: { type: string, format: uuid }
        implementationVersion: { type: string, const: csv_data_view.v1 }
        sourceRevision: { type: integer, minimum: 1 }
        sourceChecksum: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        artifactChecksum: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        artifactByteCount: { type: integer, minimum: 1, maximum: 134217728 }
        fieldCount: { type: integer, minimum: 1, maximum: 256 }
        rowCount: { type: integer, minimum: 0, maximum: 100000 }
        fields:
          type: array
          minItems: 1
          maxItems: 256
          items: { $ref: '#/components/schemas/SourceDataViewField' }
        classification: { type: string, enum: [public, internal, confidential, restricted] }
        authority: { type: string, enum: [source_of_record, supporting_reference, example] }
        audiencePolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        sensitivityPolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        purposePolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        retentionPolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        freshnessPolicyRef: { type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$' }
        freshness: { type: string, enum: [current, stale] }
        createdAt: { type: integer, minimum: 0 }
        staleAt: { type: [integer, 'null'], minimum: 0 }
      additionalProperties: false
    CreateDataViewQueryGrantInput:
      type: object
      required: [subjectId, purpose, channel, consent, fieldIds, rowOffset, rowCount, ttlSeconds]
      properties:
        subjectId: { type: string, minLength: 1, maxLength: 128 }
        purpose: { type: string, minLength: 1, maxLength: 64 }
        channel: { type: [string, 'null'], minLength: 1, maxLength: 64 }
        consent: { $ref: '#/components/schemas/AccessConsent' }
        fieldIds:
          type: array
          minItems: 1
          maxItems: 256
          uniqueItems: true
          items: { type: string, pattern: '^field\.[0-9a-f]{32}$' }
        rowOffset:
          type: integer
          minimum: 0
          maximum: 99999
          description: First row ordinal in the exact grant window.
        rowCount:
          type: integer
          minimum: 1
          maximum: 256
          description: Exact row count; a window extending beyond the current manifest rejects rather than clipping.
        ttlSeconds: { type: integer, minimum: 60, maximum: 2592000 }
      additionalProperties: false
    QuerySourceDataViewInput:
      type: object
      required: [consent, fieldIds, rowOffset, rowCount]
      properties:
        consent: { $ref: '#/components/schemas/AccessConsent' }
        fieldIds:
          type: array
          minItems: 1
          maxItems: 32
          uniqueItems: true
          items: { type: string, pattern: '^field\.[0-9a-f]{32}$' }
        rowOffset: { type: integer, minimum: 0, maximum: 99999 }
        rowCount: { type: integer, minimum: 1, maximum: 256 }
      additionalProperties: false
      description: >
        The product of fieldIds and rowCount cannot exceed 8192. Identity,
        purpose, channel, operation, autonomy and permission mode are never body fields.
    SourceDataViewSelectionRow:
      type: object
      required: [rowId, ordinal, values]
      properties:
        rowId: { type: string, pattern: '^row\.[0-9a-f]{32}$' }
        ordinal: { type: integer, minimum: 0, maximum: 99999 }
        values:
          type: array
          minItems: 1
          maxItems: 32
          items:
            type: string
            description: Inert untrusted source data; never interpreted as an instruction or expression.
      additionalProperties: false
    ProtectedSourceDataViewSelection:
      type: object
      required: [dataViewId, sourceId, sourceVersionId, sourceRevision, sourceChecksum, artifactChecksum, freshness, classification, authority, implementationVersion, rowOffset, requestedRowCount, returnedRowCount, totalRowCount, complete, fields, rows, observedAt]
      properties:
        dataViewId: { type: string, format: uuid }
        sourceId: { type: string, format: uuid }
        sourceVersionId: { type: string, format: uuid }
        sourceRevision: { type: integer, minimum: 1 }
        sourceChecksum: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        artifactChecksum: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        freshness: { type: string, const: current }
        classification: { type: string, enum: [public, internal, confidential, restricted] }
        authority: { type: string, enum: [source_of_record, supporting_reference, example] }
        implementationVersion: { type: string, const: csv_data_view_selection.v1 }
        rowOffset: { type: integer, minimum: 0, maximum: 99999 }
        requestedRowCount: { type: integer, minimum: 1, maximum: 256 }
        returnedRowCount: { type: integer, minimum: 1, maximum: 256 }
        totalRowCount: { type: integer, minimum: 1, maximum: 100000 }
        complete:
          type: boolean
          description: True only when the returned window reaches the current final row.
        fields:
          type: array
          minItems: 1
          maxItems: 32
          items: { $ref: '#/components/schemas/SourceDataViewField' }
        rows:
          type: array
          minItems: 1
          maxItems: 256
          items: { $ref: '#/components/schemas/SourceDataViewSelectionRow' }
        observedAt: { type: integer, minimum: 0 }
      additionalProperties: false
    AccessConsent:
      oneOf:
        - type: object
          required: [state]
          properties:
            state: { type: string, const: not_required }
          additionalProperties: false
        - type: object
          required: [state, evidenceId]
          properties:
            state: { type: string, const: recorded }
            evidenceId:
              type: string
              pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$'
          additionalProperties: false
    CreateAccessGrantInput:
      type: object
      required: [subjectId, purpose, channel, consent, ttlSeconds]
      properties:
        operation: { type: string, enum: [source_content.read, source_content.search] }
        subjectId: { type: string, minLength: 1, maxLength: 128 }
        purpose: { type: string, minLength: 1, maxLength: 64 }
        channel: { type: [string, 'null'], minLength: 1, maxLength: 64 }
        consent: { $ref: '#/components/schemas/AccessConsent' }
        ttlSeconds: { type: integer, minimum: 60, maximum: 2592000 }
      additionalProperties: false
    AccessGrantMutationReceipt:
      type: object
      required: [grantId, revision, mutation, acceptedAt]
      properties:
        grantId: { type: string, format: uuid }
        revision: { type: integer, minimum: 1 }
        mutation: { type: string, enum: [created, revoked] }
        acceptedAt: { type: integer, minimum: 0 }
      additionalProperties: false
    AccessGrantProjection:
      type: object
      required: [grantId, revision, state, workspaceId, profileId, subjectId, purpose, channel, resourceKind, resourceId, operation, fieldScope, rowScope, consent, autonomyCeiling, effectiveAt, expiresAt, issuedBy, revisionCreatedAt, revokedAt]
      properties:
        grantId: { type: string, format: uuid }
        revision: { type: integer, minimum: 1 }
        state: { type: string, enum: [active, revoked] }
        workspaceId: { type: string }
        profileId: { type: string }
        subjectId: { type: string }
        purpose: { type: string }
        channel: { type: [string, 'null'] }
        resourceKind: { type: string, enum: [source_resource, source_data_view] }
        resourceId: { type: string, format: uuid }
        operation: { type: string, enum: [source_content.read, source_content.search, source_data_views.query] }
        fieldScope:
          oneOf:
            - { $ref: '#/components/schemas/AllAccessScope' }
            - { $ref: '#/components/schemas/FieldListAccessScope' }
        rowScope:
          oneOf:
            - { $ref: '#/components/schemas/AllAccessScope' }
            - { $ref: '#/components/schemas/RowListAccessScope' }
        consent: { $ref: '#/components/schemas/AccessConsent' }
        autonomyCeiling: { type: string, const: observe }
        effectiveAt: { type: integer }
        expiresAt: { type: integer }
        issuedBy: { type: string, const: install_owner }
        revisionCreatedAt: { type: integer }
        revokedAt: { type: [integer, 'null'] }
    AccessGrant:
      allOf:
        - { $ref: '#/components/schemas/AccessGrantProjection' }
      unevaluatedProperties: false
    AllAccessScope:
      type: object
      required: [mode]
      properties:
        mode: { type: string, const: all }
      additionalProperties: false
    FieldListAccessScope:
      type: object
      required: [mode, ids]
      properties:
        mode: { type: string, const: list }
        ids:
          type: array
          minItems: 1
          maxItems: 256
          uniqueItems: true
          items: { type: string, pattern: '^field\.[0-9a-f]{32}$' }
      additionalProperties: false
    RowListAccessScope:
      type: object
      required: [mode, ids]
      properties:
        mode: { type: string, const: list }
        ids:
          type: array
          minItems: 1
          maxItems: 256
          uniqueItems: true
          items: { type: string, pattern: '^row\.[0-9a-f]{32}$' }
      additionalProperties: false
    CurrentAccessGrant:
      allOf:
        - { $ref: '#/components/schemas/AccessGrantProjection' }
        - type: object
          required: [lifecycle]
          properties:
            lifecycle: { type: string, enum: [scheduled, effective, expired, revoked] }
      unevaluatedProperties: false
    AccessGrantList:
      type: object
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/CurrentAccessGrant' }
        nextCursor: { type: [string, 'null'], format: uuid }
      additionalProperties: false
    ReadSourceContentInput:
      type: object
      required: [consent, byteStart, byteEnd]
      properties:
        consent: { $ref: '#/components/schemas/AccessConsent' }
        byteStart: { type: integer, minimum: 0 }
        byteEnd: { type: integer, minimum: 1 }
      additionalProperties: false
      description: Subject identity comes only from the verified delegated principal.
    ProtectedSourceContent:
      type: object
      required: [resourceId, sourceId, sourceVersionId, sourceRevision, sourceChecksum, resourceChecksum, freshness, classification, authority, text, byteStart, byteEnd, byteCount, totalByteCount, observedAt]
      properties:
        resourceId: { type: string, format: uuid }
        sourceId: { type: string, format: uuid }
        sourceVersionId: { type: string, format: uuid }
        sourceRevision: { type: integer, minimum: 1 }
        sourceChecksum: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        resourceChecksum: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        freshness: { type: string, const: current }
        classification: { type: string, enum: [public, internal, confidential, restricted] }
        authority: { type: string, enum: [source_of_record, supporting_reference, example] }
        text: { type: string }
        byteStart: { type: integer, minimum: 0 }
        byteEnd: { type: integer, minimum: 1 }
        byteCount: { type: integer, minimum: 1, maximum: 65536 }
        totalByteCount: { type: integer, minimum: 1 }
        observedAt: { type: integer, minimum: 0 }
      additionalProperties: false
    SearchSourceContentInput:
      type: object
      required: [consent, query, matchMode, maxMatches, contextBytes]
      properties:
        consent: { $ref: '#/components/schemas/AccessConsent' }
        query:
          type: string
          minLength: 1
          maxLength: 128
          description: Literal query limited to 128 UTF-8 bytes; maxLength is the outer character-count ceiling.
        matchMode: { type: string, enum: [exact_utf8, ascii_case_insensitive] }
        maxMatches: { type: integer, minimum: 1, maximum: 20 }
        contextBytes: { type: integer, minimum: 0, maximum: 1024 }
      additionalProperties: false
      description: Subject identity comes only from the verified delegated principal.
    ProtectedSourceSearchMatch:
      type: object
      required: [evidenceId, text, byteStart, byteEnd, matchByteStart, matchByteEnd]
      properties:
        evidenceId: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        text: { type: string }
        byteStart: { type: integer, minimum: 0 }
        byteEnd: { type: integer, minimum: 1 }
        matchByteStart: { type: integer, minimum: 0 }
        matchByteEnd: { type: integer, minimum: 1 }
      additionalProperties: false
    ProtectedSourceSearchResult:
      type: object
      required: [resourceId, sourceId, sourceVersionId, sourceRevision, sourceChecksum, resourceChecksum, freshness, classification, authority, status, matchMode, matches, truncated, totalByteCount, observedAt]
      properties:
        resourceId: { type: string, format: uuid }
        sourceId: { type: string, format: uuid }
        sourceVersionId: { type: string, format: uuid }
        sourceRevision: { type: integer, minimum: 1 }
        sourceChecksum: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        resourceChecksum: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        freshness: { type: string, const: current }
        classification: { type: string, enum: [public, internal, confidential, restricted] }
        authority: { type: string, enum: [source_of_record, supporting_reference, example] }
        status: { type: string, enum: [complete, no_matches] }
        matchMode: { type: string, enum: [exact_utf8, ascii_case_insensitive] }
        matches:
          type: array
          maxItems: 20
          items: { $ref: '#/components/schemas/ProtectedSourceSearchMatch' }
        truncated: { type: boolean }
        totalByteCount: { type: integer, minimum: 1 }
        observedAt:
          type: integer
          minimum: 0
          description: Evidence-snapshot creation time. Equivalent repeated searches may retain this value; it is not proof of current authorization, cache state, response time, or source freshness.
      additionalProperties: false
    SourceJobCancellationResult:
      allOf:
        - { $ref: '#/components/schemas/SourceJob' }
        - type: object
          required: [cancellation]
          properties:
            cancellation: { type: string, enum: [requested, already_requested] }
    SourceDeletionCounts:
      type: object
      required: [immutableOriginals, uploadStaging, placedCandidates, derivedResources, dataViews, searchIndexes, sourceJobs, idempotencyReplays, retrievalCacheEntries]
      properties:
        immutableOriginals: { type: integer, minimum: 0 }
        uploadStaging: { type: integer, minimum: 0 }
        placedCandidates: { type: integer, minimum: 0 }
        derivedResources: { type: integer, minimum: 0 }
        dataViews: { type: integer, minimum: 0 }
        searchIndexes: { type: integer, minimum: 0 }
        sourceJobs: { type: integer, minimum: 0 }
        idempotencyReplays: { type: integer, minimum: 0 }
        retrievalCacheEntries: { type: integer, minimum: 0 }
      additionalProperties: false
    SourceDeletionProjection:
      type: object
      required: [jobId, sourceId, operation, state, sourceRevision, affected, remaining, createdAt, updatedAt, terminalAt]
      properties:
        jobId: { type: string, format: uuid }
        sourceId: { type: string, format: uuid }
        operation: { type: string, const: delete_source }
        state:
          type: string
          enum: [queued, deleting, cancel_requested, cancelled, partially_deleted, deleted]
        sourceRevision: { type: integer, minimum: 1 }
        affected: { $ref: '#/components/schemas/SourceDeletionCounts' }
        remaining: { $ref: '#/components/schemas/SourceDeletionCounts' }
        createdAt: { type: integer }
        updatedAt: { type: integer }
        terminalAt: { type: [integer, 'null'] }
    SourceDeletion:
      allOf:
        - { $ref: '#/components/schemas/SourceDeletionProjection' }
      unevaluatedProperties: false
    SourceDeletionCancellationResult:
      allOf:
        - { $ref: '#/components/schemas/SourceDeletionProjection' }
        - type: object
          required: [cancellation]
          properties:
            cancellation: { type: string, enum: [requested, already_requested] }
      unevaluatedProperties: false
    SourceDeletionRetryResult:
      allOf:
        - { $ref: '#/components/schemas/SourceDeletionProjection' }
        - type: object
          required: [retry]
          properties:
            retry: { type: string, const: queued }
      unevaluatedProperties: false
    CandidateFinding:
      type: object
      required: [code, severity, message]
      properties:
        code: { type: string }
        severity: { type: string, enum: [error, warning] }
        message: { type: string }
        subjects:
          type: array
          items: { type: string }
      additionalProperties: false
    CandidateValidationResult:
      type: object
      required: [valid, candidateId, profileName, fileCount, totalBytes, findings]
      properties:
        valid: { type: boolean }
        candidateId:
          type: [string, 'null']
          pattern: '^sha256:[0-9a-f]{64}$'
          description: Opaque deterministic identity, present only for a valid candidate.
        profileName: { type: [string, 'null'] }
        fileCount: { type: [integer, 'null'], minimum: 0 }
        totalBytes: { type: [integer, 'null'], minimum: 0 }
        findings:
          type: array
          items: { $ref: '#/components/schemas/CandidateFinding' }
      additionalProperties: false
    CandidateStageResult:
      type: object
      required: [candidateId, profileName, state, ready, idempotent, code, fileCount, totalBytes]
      properties:
        candidateId: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        profileName: { type: string }
        state: { type: string, enum: [ready, placement_failed, cleanup_failed] }
        ready: { type: boolean }
        idempotent: { type: boolean }
        code: { type: [string, 'null'] }
        fileCount: { type: integer, minimum: 0 }
        totalBytes: { type: integer, minimum: 0 }
      additionalProperties: false
    CandidateActivationResult:
      type: object
      required: [state, changed, candidateId, previousCandidateId, activeCandidateId, deploymentRevision, routingState, health, healthObservedAt, code]
      properties:
        state: { type: string, enum: [active, activation_failed] }
        changed: { type: boolean }
        candidateId: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        previousCandidateId: { type: [string, 'null'], pattern: '^sha256:[0-9a-f]{64}$' }
        activeCandidateId: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        deploymentRevision: { type: integer, minimum: 1 }
        routingState: { type: string, enum: [active, paused] }
        health: { type: string, enum: [unknown, starting, healthy, degraded, unhealthy] }
        healthObservedAt: { type: [integer, 'null'], minimum: 0 }
        code: { type: [string, 'null'] }
      additionalProperties: false
    CandidateRollbackResult:
      type: object
      required: [state, changed, candidateId, previousCandidateId, activeCandidateId, deploymentRevision, routingState, health, healthObservedAt, code]
      properties:
        state: { type: string, enum: [rolled_back, rollback_failed] }
        changed: { type: boolean }
        candidateId: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        previousCandidateId: { type: [string, 'null'], pattern: '^sha256:[0-9a-f]{64}$' }
        activeCandidateId: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        deploymentRevision: { type: integer, minimum: 1 }
        routingState: { type: string, enum: [active, paused] }
        health: { type: string, enum: [unknown, starting, healthy, degraded, unhealthy] }
        healthObservedAt: { type: [integer, 'null'], minimum: 0 }
        code: { type: [string, 'null'] }
      additionalProperties: false
    ProfileDeploymentResult:
      type: object
      required: [state, changed, profileId, activeCandidateId, deploymentRevision, routingState, health, healthObservedAt, activeRunCount]
      properties:
        state: { type: string, enum: [active, paused] }
        changed: { type: boolean }
        profileId: { type: string }
        activeCandidateId: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        deploymentRevision: { type: integer, minimum: 1 }
        routingState: { type: string, enum: [active, paused] }
        health: { type: string, enum: [unknown, starting, healthy, degraded, unhealthy] }
        healthObservedAt: { type: [integer, 'null'], minimum: 0 }
        activeRunCount: { type: integer, minimum: 0 }
      additionalProperties: false
    CandidateStatus:
      type: object
      required: [candidateId, profileId, state, ready, fileCount, totalBytes, code, createdAt, updatedAt, deletedAt, deletionEligible, deletionBlockedBy]
      properties:
        candidateId: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        profileId: { type: string }
        state:
          type: string
          enum: [placing, ready, placement_failed, cleanup_failed, deleting, delete_failed, deleted]
        ready: { type: boolean }
        fileCount: { type: integer, minimum: 0 }
        totalBytes: { type: integer, minimum: 0 }
        code: { type: [string, 'null'] }
        createdAt: { type: integer, minimum: 0 }
        updatedAt: { type: integer, minimum: 0 }
        deletedAt: { type: [integer, 'null'], minimum: 0 }
        deletionEligible: { type: boolean }
        deletionBlockedBy:
          type: [string, 'null']
          enum: [not_found, scope_mismatch, not_ready, active, in_use, rollback_retained, in_progress, null]
      additionalProperties: false
    CandidateDeletionResult:
      type: object
      required: [candidateId, profileId, state, deleted, idempotent, code]
      properties:
        candidateId: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        profileId: { type: string }
        state: { type: string, enum: [deleted, delete_failed] }
        deleted: { type: boolean }
        idempotent: { type: boolean }
        code: { type: [string, 'null'] }
      additionalProperties: false
    ProfileDeploymentStatus:
      type: object
      required: [profileId, activeCandidateId, deploymentRevision, routingState, health, healthObservedAt, activeRunCount, updatedAt]
      properties:
        profileId: { type: string }
        activeCandidateId: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
        deploymentRevision: { type: integer, minimum: 1 }
        routingState: { type: string, enum: [active, paused] }
        health: { type: string, enum: [unknown, starting, healthy, degraded, unhealthy] }
        healthObservedAt: { type: [integer, 'null'], minimum: 0 }
        activeRunCount: { type: integer, minimum: 0 }
        updatedAt: { type: integer, minimum: 0 }
      additionalProperties: false
    PublicProfileSummary:
      type: object
      required: [id]
      properties:
        id: { type: string }
        name: { type: string }
        displayName: { type: [string, 'null'] }
        description: { type: string }
        availability: { type: string, enum: [available, paused, invalid, unavailable] }
        activeCandidateId: { type: [string, 'null'], pattern: '^sha256:[0-9a-f]{64}$' }
        deploymentRevision: { type: [integer, 'null'], minimum: 1 }
        health: { type: string, enum: [unknown, starting, healthy, degraded, unhealthy] }
        healthObservedAt: { type: [integer, 'null'], minimum: 0 }
        requiredCapabilities: { type: array, items: { type: string } }
        findings: { type: array, items: { $ref: '#/components/schemas/CandidateFinding' } }
      description: Owner responses may contain additional internal-management fields; delegated responses contain only this safe projection.
    DelegatedPrincipal:
      type: object
      required: [kind, tokenId, delegateId, workspaceId, profileId, purpose, operations, issuedAt, expiresAt]
      properties:
        kind: { type: string, const: delegated }
        tokenId: { type: string, format: uuid }
        delegateId: { type: string }
        subjectId:
          type: string
          description: Explicit grant subject when this delegated principal is subject-bound.
        workspaceId: { type: string }
        profileId: { type: string }
        purpose: { type: string }
        channel: { type: string }
        operations: { type: array, items: { type: string } }
        issuedAt: { type: integer, description: Unix seconds. }
        expiresAt: { type: integer, description: Unix seconds. }
    ErrorResponse:
      type: object
      required: [error, message, category, correlationId]
      properties:
        error: { type: string, pattern: '^[a-z][a-z0-9_]{0,63}$' }
        message: { type: string, maxLength: 500 }
        category: { type: string, pattern: '^[a-z][a-z0-9_]{0,63}$' }
        correlationId: { type: string, format: uuid }
        retryAfter:
          type: number
          minimum: 0
          description: Optional retry delay in seconds.
        actualRevision:
          type: integer
          minimum: 1
          description: Safe actual source revision, present only for `source_upload_refresh_conflict`.
        actualCurrentVersionId:
          type: [string, 'null']
          format: uuid
          description: Safe actual current source version, present only for `source_upload_refresh_conflict`.
    SourceDeletionError:
      type: object
      required: [error, message, category, correlationId]
      properties:
        error: { type: string, pattern: '^[a-z][a-z0-9_]{0,63}$' }
        message: { type: string, maxLength: 500 }
        category: { type: string, pattern: '^[a-z][a-z0-9_]{0,63}$' }
        correlationId: { type: string, format: uuid }
        retryAfter: { type: number, minimum: 0 }
        limitBytes:
          type: integer
          minimum: 1
          description: Public JSON body ceiling, present only when the request is too large.
      additionalProperties: false
    SourceQuotaExceededError:
      type: object
      required: [error, message, category, correlationId, resourceClass]
      description: >
        Quota denial without usage, failed scope, source/upload/job/resource
        identities, paths, or retry timing.
      properties:
        error: { type: string, const: source_quota_exceeded }
        message: { type: string, const: Source quota does not allow this operation. }
        category: { type: string, const: invalid_request }
        correlationId: { type: string, format: uuid }
        resourceClass:
          type: string
          enum: [source_registrations, source_storage_bytes, source_upload_sessions, source_jobs, source_derived_resources]
      additionalProperties: false
  securitySchemes:
    bearer:
      type: http
      scheme: bearer
      description: >
        Gateway session token. Enabled automatically on any non-loopback
        bind; persisted at <dataDir>/gateway-token (0600).
