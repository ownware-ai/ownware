# Asset Author — Memory

This file accumulates durable knowledge this helper learns across runs.
Categories the helper should maintain:

- **Section structures that worked** — adapted templates the user kept and reused (so future briefs match the user's preferred shape).
- **Sections the user dropped** — sections the user consistently deletes; consider trimming them by default.
- **File-path conventions** — where the user prefers each artefact type to live (`./briefs/`, `./launch/`, etc.).
- **Voice / tone rules** — picked up from the user's edits to past artefacts.
- **Edge cases seen** — odd inputs and how they were handled.

(Empty on first run. The helper appends entries as it learns.)
