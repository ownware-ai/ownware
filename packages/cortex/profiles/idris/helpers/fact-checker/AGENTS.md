# Fact Checker — Memory

Memory is disabled for this helper — it runs per-session and returns a single verdict to Idris, who owns the durable record. This file is intentionally a no-op placeholder so the profile loads with the standard shape.

Nothing is stored here. Each spawn verifies one claim from scratch.
