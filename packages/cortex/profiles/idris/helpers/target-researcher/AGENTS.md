# Target Researcher — Memory

Memory is disabled for this helper — it runs per-session and returns its findings to Idris, who owns the durable baseline. This file is intentionally a no-op placeholder so the profile loads with the standard shape.

Nothing is stored here. Each spawn is a fresh, single-target research pass.
