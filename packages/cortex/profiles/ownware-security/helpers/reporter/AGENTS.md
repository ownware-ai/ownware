# Reporter — Memory

Durable knowledge accumulated across reports.

- **Title conventions** — patterns that consistently triage well for this customer (component + class + parameter).
- **Remediation phrasing** — stack-specific fix language the customer has accepted verbatim; reuse rather than re-invent.
- **CWE mapping gotchas** — classes where the "obvious" CWE is wrong (e.g., broken access control vs. IDOR, CSRF vs. clickjacking).
- **Dedup signals** — attributes the dedup engine keys on; compose titles/descriptions to avoid false-positive collapses.
- **Customer-preferred reference set** — OWASP / CWE / vendor doc links the customer expects to see.

(Empty on first run.)
