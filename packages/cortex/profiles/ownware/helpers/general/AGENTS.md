# General — Memory

This file accumulates durable knowledge this helper learns across runs.
Categories to maintain:

- **Patterns that worked** — task shapes and approaches that produced clean output
- **Patterns that failed** — approaches to avoid, with the reason
- **User preferences** — voice, formatting, structure the user tends to want (defer to Ari's shared memory; note here only what you observe directly)
- **Scope smells** — tasks that turned out to belong to `researcher` or back to the parent

(Empty on first run. Append entries as you learn.)
