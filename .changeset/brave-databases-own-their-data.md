---
"@ownware/cortex": minor
"ownware": minor
---

Add strict pluggable gateway storage with zero-config SQLite, explicitly selected tenant-owned PostgreSQL, immutable cross-adapter migrations, and transactionally ordered durable message history.
